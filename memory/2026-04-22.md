# 2026-04-22

## Gece ek notlar
- Prisma local bootstrap hatti tekrar kontrol edildi; `npm run prisma:bootstrap:local` komutu `.env` veya `.env.local` icinden `DATABASE_URL` okuyup `npx prisma migrate deploy` ve `npx prisma generate` akisini tek komutta kosacak sekilde hazir durumda.
- Dogrulama icin helper `bash -n` ile syntax kontrolunden gecirildi, `./bin/bootstrap-prisma-local --dry-run` beklenen ciktiyi verdi ve uygulama tarafinda `npm run build` tekrar gecti.
- Gercek DB apply hala bloklu; workspace icinde henuz kullanilabilir bir `DATABASE_URL` yok.

## Flush ek notu
- Prisma bootstrap yardimci hatti package script ve `.env.example` yonlendirmesiyle baglandi; calisma yolu `npm run prisma:bootstrap:local` olarak sabitlendi.
- Bu dogrulama ve kayit `e5c726f` commit'i ile tutuldu: `Record Prisma bootstrap verification`.

## Sabah ek notlar
- `npm run deploy:hesap-merkezi-v2` calistirildi; build gecti ve `esnafdijital-admin.service` yeni build ile restart edildi.
- Deploy script icindeki ilk verify denemesi restart sonrasi kisa bir gecikmede `Connection refused` aldi, fakat servis hemen sonra saglikli sekilde ayaga kalkti.
- Ardindan `./bin/verify-hesap-merkezi-v2` yeniden calistirildi ve `health`, `hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri basariyla gecti.
- Local root HTML yeni chunk referanslariyla donuyor ve bagli CSS asset'i `200 OK` veriyor; onceki stale HTML / missing CSS durumu kapandi.
- Admin menuden `Eski ekran > Profil araçları` girisi kaldirildi ve `/codex-profilleri` route'u silindi; build dogrulamasinda bu sayfa artik route listesinde yer almiyor.
- Bu kaldirma prod'a deploy edildi; servis restart sonrasi yeniden dogrulandi ve `/codex-profilleri` localde `404 Not Found` donmeye basladi.
- Consultation Center V1 sade prompt akisi once prompt/context kurallariyla guncellendi ve prod'a deploy edildi; hedef model secimi, otomatik prompt/context kurulumu ve sifir-hafiza + VPS/OpenClaw baglamli prompt uretimi canliya alindi.
- Bu deploy sonrasi `api/health`, `api/hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri yeniden gecti; restart sirasinda gorulen kisa `Connection refused` gecici startup anina aitti.

## Consultation Center agent trigger ek notu
- Consultation Center create akisi tekrar duzeltildi; yeni kayit artik promptu direkt gostermiyor, once `preparing` durumuyla aciliyor.
- Detail acilisinda `/api/consultation-center/[id]/suggest` agent hattini tetikliyor; agent brief’i zenginlestirdikten sonra prompt kayda yaziliyor ve durum `ready` oluyor, hata varsa `error` olarak gorunuyor.
- Inbox ve detail yuzeylerinde `preparing / ready / error` prompt durumu gorunur hale getirildi; smoke testte create sonrasi `promptStatus=preparing`, suggest sonrasi `promptStatus=ready` ve prompt uzunlugu `4405` karakter olarak dogrulandi.
- Bu duzeltme build ve deploy sonrasi dogrulandi; ilgili commit `eba990e`: `Record agent-triggered consultation prompt flow`.

## Consultation Center isolated agent ek notu
- Prompt hazirlama akisinda gorulen hata, admin uygulamasinin kendi icinden ayni `main` agent session hattini tekrar `openclaw agent` ile cagirmasindan kaynaklandi; hata izi `session file locked` ve ardindan `gateway timeout` olarak goruldu.
- Prompt hazirlama hattinda self-call yerine ayrik `esnafdijital` agent kullanimi sabitlendi; ayni session lock riski bu akistan cikarildi.
- Build ve deploy tekrar alindi; ardindan `consult_1776850238725` kaydi uzerinde suggest akisi yeniden calistirildi ve `promptStatus=ready`, `promptError=null`, `promptTextLength=2716` ile dogrulandi.
# 2026-04-22

## Gece ek notlar
- Prisma local bootstrap hatti tekrar kontrol edildi; `npm run prisma:bootstrap:local` komutu `.env` veya `.env.local` icinden `DATABASE_URL` okuyup `npx prisma migrate deploy` ve `npx prisma generate` akisini tek komutta kosacak sekilde hazir durumda.
- Dogrulama icin helper `bash -n` ile syntax kontrolunden gecirildi, `./bin/bootstrap-prisma-local --dry-run` beklenen ciktiyi verdi ve uygulama tarafinda `npm run build` tekrar gecti.
- Gercek DB apply hala bloklu; workspace icinde henuz kullanilabilir bir `DATABASE_URL` yok.

## Flush ek notu
- Prisma bootstrap yardimci hatti package script ve `.env.example` yonlendirmesiyle baglandi; calisma yolu `npm run prisma:bootstrap:local` olarak sabitlendi.
- Bu dogrulama ve kayit `e5c726f` commit'i ile tutuldu: `Record Prisma bootstrap verification`.

## Sabah ek notlar
- `npm run deploy:hesap-merkezi-v2` calistirildi; build gecti ve `esnafdijital-admin.service` yeni build ile restart edildi.
- Deploy script icindeki ilk verify denemesi restart sonrasi kisa bir gecikmede `Connection refused` aldi, fakat servis hemen sonra saglikli sekilde ayaga kalkti.
- Ardindan `./bin/verify-hesap-merkezi-v2` yeniden calistirildi ve `health`, `hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri basariyla gecti.
- Local root HTML yeni chunk referanslariyla donuyor ve bagli CSS asset'i `200 OK` veriyor; onceki stale HTML / missing CSS durumu kapandi.
- Admin menuden `Eski ekran > Profil araçları` girisi kaldirildi ve `/codex-profilleri` route'u silindi; build dogrulamasinda bu sayfa artik route listesinde yer almiyor.
- Bu kaldirma prod'a deploy edildi; servis restart sonrasi yeniden dogrulandi ve `/codex-profilleri` localde `404 Not Found` donmeye basladi.
- Consultation Center V1 sade prompt akisi once prompt/context kurallariyla guncellendi ve prod'a deploy edildi; hedef model secimi, otomatik prompt/context kurulumu ve sifir-hafiza + VPS/OpenClaw baglamli prompt uretimi canliya alindi.
- Bu deploy sonrasi `api/health`, `api/hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri yeniden gecti; restart sirasinda gorulen kisa `Connection refused` gecici startup anina aitti.

## Consultation Center agent trigger ek notu
- Consultation Center create akisi tekrar duzeltildi; yeni kayit artik promptu direkt gostermiyor, once `preparing` durumuyla aciliyor.
- Detail acilisinda `/api/consultation-center/[id]/suggest` agent hattini tetikliyor; agent brief’i zenginlestirdikten sonra prompt kayda yaziliyor ve durum `ready` oluyor, hata varsa `error` olarak gorunuyor.
- Inbox ve detail yuzeylerinde `preparing / ready / error` prompt durumu gorunur hale getirildi; smoke testte create sonrasi `promptStatus=preparing`, suggest sonrasi `promptStatus=ready` ve prompt uzunlugu `4405` karakter olarak dogrulandi.
- Bu duzeltme build ve deploy sonrasi dogrulandi; ilgili commit `eba990e`: `Record agent-triggered consultation prompt flow`.

## Consultation Center isolated agent ek notu
- Prompt hazirlama akisinda gorulen hata, admin uygulamasinin kendi icinden ayni `main` agent session hattini tekrar `openclaw agent` ile cagirmasindan kaynaklandi; hata izi `session file locked` ve ardindan `gateway timeout` olarak goruldu.
- Prompt hazirlama hattinda self-call yerine ayrik `esnafdijital` agent kullanimi sabitlendi; ayni session lock riski bu akistan cikarildi.
- Build ve deploy tekrar alindi; ardindan `consult_1776850238725` kaydi uzerinde suggest akisi yeniden calistirildi ve `promptStatus=ready`, `promptError=null`, `promptTextLength=2716` ile dogrulandi.

## Oglen oncesi flush ek notu
- Isolated agent duzeltmesi sonrasi mock guncelleme hattinda eski `promptError` degerinin basarili suggest sonrasi ustte asili kalabildigi goruldu; mock update mantigi temizlenerek basarili prompt hazirlamada hata alani `null` kalacak sekilde duzeltildi.
- Bu duzeltme ilk denemede TypeScript build hatasi verdi, ardindan duzeltilip yeniden build/deploy alindi; son verify kontrolleri tekrar gecti.
- Consultation Center prompt hazirlama akisi yeniden smoke test edildi ve kayit `promptStatus=ready`, `promptError=null`, `promptTextLength=2821` durumuyla dogrulandi.
- Prompt agent secimi kod icindeki sabit fallback'tan cikarildi; `CONSULTATION_PROMPT_AGENT_ID` env ayari zorunlu hale getirildi, `.env.example` belgelendi ve local runtime `.env.local` ile dogrulandi.
- GPT-5 Prompting Guide icin yerel calisma ozeti `REFERENCES/prompting/gpt5-prompting-principles.md` olarak eklendi; Consultation Center prompt ureten hat bu yerel referansi okuyup prompt olusturma agentina baglam olarak geciyor.
- Prompting referansi icin kod ici fallback kaldirildi; `REFERENCES/prompting/gpt5-prompting-principles.md` okunamaz veya bossa prompt hazirlama artik sessiz kalite dususu yerine acik hata veriyor.
- `skills/consultation-prompt-builder/` altinda yeni workspace skill olusturuldu; Consultation Center promptlarini consultation kaydindan uretme, duzeltme ve grounding kontrolu icin SKILL.md + iki reference dosyasi yazildi ve `consultation-prompt-builder.skill` olarak paket dogrulamasi gecti.
- App tarafindaki Consultation Center prompt hatti yeni skill'e baglandi; `agent-workspace/lib/consultation-center/agent.ts` artik `skills/consultation-prompt-builder/` altindaki SKILL ve reference dosyalarini dogrudan okuyup agent promptuna gomuyor. Build/deploy ve suggest smoke test yeniden gecti.
- Consultation prompt skill ve bagli app talimati tekrar duzeltildi; sifir-hafiza varsayimi artik ic kural olarak korunuyor ama final prompta meta cumle olarak yazilmiyor, bunun yerine gerekli baglam dogrudan prompta yerlestiriliyor. Ayrica teknik olmayan kisa ozet istegi tekrar zorunlu hale getirildi; smoke testte meta sifir-hafiza cumlesi cikmadi ve plain summary istegi promptta goruldu.
- Sorunun kokunu azaltmak icin aktif runtime ve skill dosyalarindan ornek meta cumle metni tamamen kaldirildi; agent talimati ve skill referanslari artik bu ifadeyi isim vererek anmiyor. Yeni suggest smoke testte ne tam ifade ne de oturum/baglam eksigi meta dili prompta sizmadi.
- Consultation prompt akisi bir adim daha sadeleştirildi: davranis/policy kurallari skill/reference tarafinda ana kaynak haline getirildi, `agent-workspace/lib/consultation-center/agent.ts` ise daha cok skill icerigini yukleyen ve uygulama JSON kontratini ileten ince bir loader katmanina indirildi. Build, deploy ve suggest smoke test tekrar gecti; prompt `ready` dondu ve rol + iki repo linki + teknik olmayan ozet istegi korunarak uretildi.
- `agent.ts` bir tur daha inceltildi; prompt icindeki aciklayici bootstrap ve uzun JSON sekil blogu kaldirildi, consultation payload secici hale getirildi ve bos context/brief bloklari artik prompta gereksiz dump edilmiyor. Build/deploy ve yeni gecici kayitla suggest smoke test tekrar gecti; prompt `ready` dondu, rol/repo linkleri/teknik olmayan ozet istegi korundu.
- Son inceltme turunda `agent.ts` promptundan bootstrap cumleleri ve prompt icindeki uygulama kontrati blogu da tamamen kaldirildi; skill/reference + kisa consultation payload hattina inildi. Build/deploy ve yeni gecici kayitla suggest smoke test yine gecti; prompt `ready` dondu ve rol, iki repo linki, teknik olmayan ozet istegi korunarak uretildi.
- Consultation prompt skill kurali tekrar netlestirildi: teknik olmayan kisa ozet artik JSON icinde `shortPlainSummary` benzeri alan olarak istenmiyor, final promptta JSON disinda ayri ikinci bolum olarak isteniyor. Yeni suggest smoke testte prompt `ready` dondu, JSON disi ozet dili goruldu ve `shortPlainSummary` izi kayboldu.
- Citation/footnote stili de skill seviyesinde yasaklandi; `consultation-prompt-builder` artik `[1]`, `[2]`, `([GitHub][1])` ve benzeri atif formatlarini reddediyor, link gerekiyorsa dogrudan URL istiyor. Yeni suggest smoke testte prompt `ready` dondu, citation izi gorulmedi ve iki repo linki duz URL olarak korundu.
- Consultation Center quick create bolumu acilip kapanabilir hale getirildi; ustteki istek metni karti artik kullanici isterse kapatilabiliyor. Build ve deploy tekrar gecti.
- Prompt hazir olduktan sonra `Degisiklik oner` akisi eklendi; kullanici promptta neyin degismesi gerektigini yazip ayni kayit uzerinden yeniden suggest tetikleyebiliyor. Suggest hattina `changeRequest` destegi eklendi, mevcut prompt bu istekle birlikte agente veriliyor ve son istek `sharedBrief.promptRevisionRequest` altinda tutuluyor. Build/deploy ve gecici kayitla smoke test gecti; ikinci suggest sonrasi prompt gercekten degisti ve kisaldi.
- Consultation prompt skill cizgisi tekrar netlestirildi: baglam secimi artik sayi siniri ile degil, istenen degisikligi guvenle kurmaya yetecek tum baglam mantigi ile tanimlaniyor. `contextRefs` limitinin sadece kaydedilen ana kaynak ozeti oldugu aciklandi, anti-pattern olarak baglami yapay sayi siniriyla daraltma eklendi ve grounding checklist bu yeni ilkeye gore guncellendi. Suggest smoke test yine gecti.
# 2026-04-22

## Flush ek notu
- Consultation Center prompt hattinda yeni kalici cizgi netlesti: validator once gelmeyecek; once prompt ciktisinin kendisi daraltilacak ve kok sorunlar prompt kaynaginda duzeltilecek.
- Kullanici, `Bu oturum sifir hafizali kabul edilsin` benzeri meta dilin semptom kontroluyle degil, kaynagindan kaldirilmasini istedi; bu nedenle runtime ve skill talimatlarinda meta-hafiza dilini tamamen anmama cizgisi benimsendi.
- Son smoke testler bu temizligi dogruladi: prompt `ready` dondu, tam ifade yoktu, bellek/oturum meta dili gorunmedi, teknik olmayan ozet istegi korundu.
- Bu turdaki ana commitler: `7e6344e` (`Bind consultation prompt flow to skill files`), `7dba523` (`Refine consultation prompt skill writing rules`), `39051f9` (`Remove consultation prompt memory-meta wording`).
- Dis degerlendirme icin pratik sonuc cikarildi: varsayimsal validator tartismasindan once `agent.ts` icinde kalan urun/yazim kurallarini skill/reference katmanina tasimak ve gercek consultation ornekleriyle golden sample testi hazirlamak daha dogru sonraki adim olarak goruluyor.
# 2026-04-22

## Gece ek notlar
- Prisma local bootstrap hatti tekrar kontrol edildi; `npm run prisma:bootstrap:local` komutu `.env` veya `.env.local` icinden `DATABASE_URL` okuyup `npx prisma migrate deploy` ve `npx prisma generate` akisini tek komutta kosacak sekilde hazir durumda.
- Dogrulama icin helper `bash -n` ile syntax kontrolunden gecirildi, `./bin/bootstrap-prisma-local --dry-run` beklenen ciktiyi verdi ve uygulama tarafinda `npm run build` tekrar gecti.
- Gercek DB apply hala bloklu; workspace icinde henuz kullanilabilir bir `DATABASE_URL` yok.

## Flush ek notu
- Prisma bootstrap yardimci hatti package script ve `.env.example` yonlendirmesiyle baglandi; calisma yolu `npm run prisma:bootstrap:local` olarak sabitlendi.
- Bu dogrulama ve kayit `e5c726f` commit'i ile tutuldu: `Record Prisma bootstrap verification`.

## Sabah ek notlar
- `npm run deploy:hesap-merkezi-v2` calistirildi; build gecti ve `esnafdijital-admin.service` yeni build ile restart edildi.
- Deploy script icindeki ilk verify denemesi restart sonrasi kisa bir gecikmede `Connection refused` aldi, fakat servis hemen sonra saglikli sekilde ayaga kalkti.
- Ardindan `./bin/verify-hesap-merkezi-v2` yeniden calistirildi ve `health`, `hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri basariyla gecti.
- Local root HTML yeni chunk referanslariyla donuyor ve bagli CSS asset'i `200 OK` veriyor; onceki stale HTML / missing CSS durumu kapandi.
- Admin menuden `Eski ekran > Profil araçları` girisi kaldirildi ve `/codex-profilleri` route'u silindi; build dogrulamasinda bu sayfa artik route listesinde yer almiyor.
- Bu kaldirma prod'a deploy edildi; servis restart sonrasi yeniden dogrulandi ve `/codex-profilleri` localde `404 Not Found` donmeye basladi.
- Consultation Center V1 sade prompt akisi once prompt/context kurallariyla guncellendi ve prod'a deploy edildi; hedef model secimi, otomatik prompt/context kurulumu ve sifir-hafiza + VPS/OpenClaw baglamli prompt uretimi canliya alindi.
- Bu deploy sonrasi `api/health`, `api/hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri yeniden gecti; restart sirasinda gorulen kisa `Connection refused` gecici startup anina aitti.

## Consultation Center agent trigger ek notu
- Consultation Center create akisi tekrar duzeltildi; yeni kayit artik promptu direkt gostermiyor, once `preparing` durumuyla aciliyor.
- Detail acilisinda `/api/consultation-center/[id]/suggest` agent hattini tetikliyor; agent brief’i zenginlestirdikten sonra prompt kayda yaziliyor ve durum `ready` oluyor, hata varsa `error` olarak gorunuyor.
- Inbox ve detail yuzeylerinde `preparing / ready / error` prompt durumu gorunur hale getirildi; smoke testte create sonrasi `promptStatus=preparing`, suggest sonrasi `promptStatus=ready` ve prompt uzunlugu `4405` karakter olarak dogrulandi.
- Bu duzeltme build ve deploy sonrasi dogrulandi; ilgili commit `eba990e`: `Record agent-triggered consultation prompt flow`.

## Consultation Center isolated agent ek notu
- Prompt hazirlama akisinda gorulen hata, admin uygulamasinin kendi icinden ayni `main` agent session hattini tekrar `openclaw agent` ile cagirmasindan kaynaklandi; hata izi `session file locked` ve ardindan `gateway timeout` olarak goruldu.
- Prompt hazirlama hattinda self-call yerine ayrik `esnafdijital` agent kullanimi sabitlendi; ayni session lock riski bu akistan cikarildi.
- Build ve deploy tekrar alindi; ardindan `consult_1776850238725` kaydi uzerinde suggest akisi yeniden calistirildi ve `promptStatus=ready`, `promptError=null`, `promptTextLength=2716` ile dogrulandi.
# 2026-04-22

## Gece ek notlar
- Prisma local bootstrap hatti tekrar kontrol edildi; `npm run prisma:bootstrap:local` komutu `.env` veya `.env.local` icinden `DATABASE_URL` okuyup `npx prisma migrate deploy` ve `npx prisma generate` akisini tek komutta kosacak sekilde hazir durumda.
- Dogrulama icin helper `bash -n` ile syntax kontrolunden gecirildi, `./bin/bootstrap-prisma-local --dry-run` beklenen ciktiyi verdi ve uygulama tarafinda `npm run build` tekrar gecti.
- Gercek DB apply hala bloklu; workspace icinde henuz kullanilabilir bir `DATABASE_URL` yok.

## Flush ek notu
- Prisma bootstrap yardimci hatti package script ve `.env.example` yonlendirmesiyle baglandi; calisma yolu `npm run prisma:bootstrap:local` olarak sabitlendi.
- Bu dogrulama ve kayit `e5c726f` commit'i ile tutuldu: `Record Prisma bootstrap verification`.

## Sabah ek notlar
- `npm run deploy:hesap-merkezi-v2` calistirildi; build gecti ve `esnafdijital-admin.service` yeni build ile restart edildi.
- Deploy script icindeki ilk verify denemesi restart sonrasi kisa bir gecikmede `Connection refused` aldi, fakat servis hemen sonra saglikli sekilde ayaga kalkti.
- Ardindan `./bin/verify-hesap-merkezi-v2` yeniden calistirildi ve `health`, `hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri basariyla gecti.
- Local root HTML yeni chunk referanslariyla donuyor ve bagli CSS asset'i `200 OK` veriyor; onceki stale HTML / missing CSS durumu kapandi.
- Admin menuden `Eski ekran > Profil araçları` girisi kaldirildi ve `/codex-profilleri` route'u silindi; build dogrulamasinda bu sayfa artik route listesinde yer almiyor.
- Bu kaldirma prod'a deploy edildi; servis restart sonrasi yeniden dogrulandi ve `/codex-profilleri` localde `404 Not Found` donmeye basladi.
- Consultation Center V1 sade prompt akisi once prompt/context kurallariyla guncellendi ve prod'a deploy edildi; hedef model secimi, otomatik prompt/context kurulumu ve sifir-hafiza + VPS/OpenClaw baglamli prompt uretimi canliya alindi.
- Bu deploy sonrasi `api/health`, `api/hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri yeniden gecti; restart sirasinda gorulen kisa `Connection refused` gecici startup anina aitti.

## Consultation Center agent trigger ek notu
- Consultation Center create akisi tekrar duzeltildi; yeni kayit artik promptu direkt gostermiyor, once `preparing` durumuyla aciliyor.
- Detail acilisinda `/api/consultation-center/[id]/suggest` agent hattini tetikliyor; agent brief’i zenginlestirdikten sonra prompt kayda yaziliyor ve durum `ready` oluyor, hata varsa `error` olarak gorunuyor.
- Inbox ve detail yuzeylerinde `preparing / ready / error` prompt durumu gorunur hale getirildi; smoke testte create sonrasi `promptStatus=preparing`, suggest sonrasi `promptStatus=ready` ve prompt uzunlugu `4405` karakter olarak dogrulandi.
- Bu duzeltme build ve deploy sonrasi dogrulandi; ilgili commit `eba990e`: `Record agent-triggered consultation prompt flow`.

## Consultation Center isolated agent ek notu
- Prompt hazirlama akisinda gorulen hata, admin uygulamasinin kendi icinden ayni `main` agent session hattini tekrar `openclaw agent` ile cagirmasindan kaynaklandi; hata izi `session file locked` ve ardindan `gateway timeout` olarak goruldu.
- Prompt hazirlama hattinda self-call yerine ayrik `esnafdijital` agent kullanimi sabitlendi; ayni session lock riski bu akistan cikarildi.
- Build ve deploy tekrar alindi; ardindan `consult_1776850238725` kaydi uzerinde suggest akisi yeniden calistirildi ve `promptStatus=ready`, `promptError=null`, `promptTextLength=2716` ile dogrulandi.

## Oglen oncesi flush ek notu
- Isolated agent duzeltmesi sonrasi mock guncelleme hattinda eski `promptError` degerinin basarili suggest sonrasi ustte asili kalabildigi goruldu; mock update mantigi temizlenerek basarili prompt hazirlamada hata alani `null` kalacak sekilde duzeltildi.
- Bu duzeltme ilk denemede TypeScript build hatasi verdi, ardindan duzeltilip yeniden build/deploy alindi; son verify kontrolleri tekrar gecti.
- Consultation Center prompt hazirlama akisi yeniden smoke test edildi ve kayit `promptStatus=ready`, `promptError=null`, `promptTextLength=2821` durumuyla dogrulandi.
- Prompt agent secimi kod icindeki sabit fallback'tan cikarildi; `CONSULTATION_PROMPT_AGENT_ID` env ayari zorunlu hale getirildi, `.env.example` belgelendi ve local runtime `.env.local` ile dogrulandi.
- GPT-5 Prompting Guide icin yerel calisma ozeti `REFERENCES/prompting/gpt5-prompting-principles.md` olarak eklendi; Consultation Center prompt ureten hat bu yerel referansi okuyup prompt olusturma agentina baglam olarak geciyor.
- Prompting referansi icin kod ici fallback kaldirildi; `REFERENCES/prompting/gpt5-prompting-principles.md` okunamaz veya bossa prompt hazirlama artik sessiz kalite dususu yerine acik hata veriyor.
- `skills/consultation-prompt-builder/` altinda yeni workspace skill olusturuldu; Consultation Center promptlarini consultation kaydindan uretme, duzeltme ve grounding kontrolu icin SKILL.md + iki reference dosyasi yazildi ve `consultation-prompt-builder.skill` olarak paket dogrulamasi gecti.
- App tarafindaki Consultation Center prompt hatti yeni skill'e baglandi; `agent-workspace/lib/consultation-center/agent.ts` artik `skills/consultation-prompt-builder/` altindaki SKILL ve reference dosyalarini dogrudan okuyup agent promptuna gomuyor. Build/deploy ve suggest smoke test yeniden gecti.
- Consultation prompt skill ve bagli app talimati tekrar duzeltildi; sifir-hafiza varsayimi artik ic kural olarak korunuyor ama final prompta meta cumle olarak yazilmiyor, bunun yerine gerekli baglam dogrudan prompta yerlestiriliyor. Ayrica teknik olmayan kisa ozet istegi tekrar zorunlu hale getirildi; smoke testte meta sifir-hafiza cumlesi cikmadi ve plain summary istegi promptta goruldu.
- Sorunun kokunu azaltmak icin aktif runtime ve skill dosyalarindan ornek meta cumle metni tamamen kaldirildi; agent talimati ve skill referanslari artik bu ifadeyi isim vererek anmiyor. Yeni suggest smoke testte ne tam ifade ne de oturum/baglam eksigi meta dili prompta sizmadi.
- Consultation prompt akisi bir adim daha sadeleştirildi: davranis/policy kurallari skill/reference tarafinda ana kaynak haline getirildi, `agent-workspace/lib/consultation-center/agent.ts` ise daha cok skill icerigini yukleyen ve uygulama JSON kontratini ileten ince bir loader katmanina indirildi. Build, deploy ve suggest smoke test tekrar gecti; prompt `ready` dondu ve rol + iki repo linki + teknik olmayan ozet istegi korunarak uretildi.
- `agent.ts` bir tur daha inceltildi; prompt icindeki aciklayici bootstrap ve uzun JSON sekil blogu kaldirildi, consultation payload secici hale getirildi ve bos context/brief bloklari artik prompta gereksiz dump edilmiyor. Build/deploy ve yeni gecici kayitla suggest smoke test tekrar gecti; prompt `ready` dondu, rol/repo linkleri/teknik olmayan ozet istegi korundu.
- Son inceltme turunda `agent.ts` promptundan bootstrap cumleleri ve prompt icindeki uygulama kontrati blogu da tamamen kaldirildi; skill/reference + kisa consultation payload hattina inildi. Build/deploy ve yeni gecici kayitla suggest smoke test yine gecti; prompt `ready` dondu ve rol, iki repo linki, teknik olmayan ozet istegi korunarak uretildi.
- Consultation prompt skill kurali tekrar netlestirildi: teknik olmayan kisa ozet artik JSON icinde `shortPlainSummary` benzeri alan olarak istenmiyor, final promptta JSON disinda ayri ikinci bolum olarak isteniyor. Yeni suggest smoke testte prompt `ready` dondu, JSON disi ozet dili goruldu ve `shortPlainSummary` izi kayboldu.
- Citation/footnote stili de skill seviyesinde yasaklandi; `consultation-prompt-builder` artik `[1]`, `[2]`, `([GitHub][1])` ve benzeri atif formatlarini reddediyor, link gerekiyorsa dogrudan URL istiyor. Yeni suggest smoke testte prompt `ready` dondu, citation izi gorulmedi ve iki repo linki duz URL olarak korundu.
- Consultation Center quick create bolumu acilip kapanabilir hale getirildi; ustteki istek metni karti artik kullanici isterse kapatilabiliyor. Build ve deploy tekrar gecti.
- Prompt hazir olduktan sonra `Degisiklik oner` akisi eklendi; kullanici promptta neyin degismesi gerektigini yazip ayni kayit uzerinden yeniden suggest tetikleyebiliyor. Suggest hattina `changeRequest` destegi eklendi, mevcut prompt bu istekle birlikte agente veriliyor ve son istek `sharedBrief.promptRevisionRequest` altinda tutuluyor. Build/deploy ve gecici kayitla smoke test gecti; ikinci suggest sonrasi prompt gercekten degisti ve kisaldi.
- Consultation prompt skill cizgisi tekrar netlestirildi: baglam secimi artik sayi siniri ile degil, istenen degisikligi guvenle kurmaya yetecek tum baglam mantigi ile tanimlaniyor. `contextRefs` limitinin sadece kaydedilen ana kaynak ozeti oldugu aciklandi, anti-pattern olarak baglami yapay sayi siniriyla daraltma eklendi ve grounding checklist bu yeni ilkeye gore guncellendi. Suggest smoke test yine gecti.
# 2026-04-22

## Flush ek notu
- Consultation Center prompt hattinda yeni kalici cizgi netlesti: validator once gelmeyecek; once prompt ciktisinin kendisi daraltilacak ve kok sorunlar prompt kaynaginda duzeltilecek.
- Kullanici, `Bu oturum sifir hafizali kabul edilsin` benzeri meta dilin semptom kontroluyle degil, kaynagindan kaldirilmasini istedi; bu nedenle runtime ve skill talimatlarinda meta-hafiza dilini tamamen anmama cizgisi benimsendi.
- Son smoke testler bu temizligi dogruladi: prompt `ready` dondu, tam ifade yoktu, bellek/oturum meta dili gorunmedi, teknik olmayan ozet istegi korundu.
- Bu turdaki ana commitler: `7e6344e` (`Bind consultation prompt flow to skill files`), `7dba523` (`Refine consultation prompt skill writing rules`), `39051f9` (`Remove consultation prompt memory-meta wording`).
- Dis degerlendirme icin pratik sonuc cikarildi: varsayimsal validator tartismasindan once `agent.ts` icinde kalan urun/yazim kurallarini skill/reference katmanina tasimak ve gercek consultation ornekleriyle golden sample testi hazirlamak daha dogru sonraki adim olarak goruluyor.

## Ogleden sonra flush ek notu
- Consultation prompt skill'i icin sinir yazimi netlestirildi: `Kucuk isletme, sade MVP ve dusuk operasyon yuku cizgisini koru.` ifadesi ayri bir zorunlu constraint olarak tutulmak zorunda degil, cunku proje baglaminda zaten tasiniyor.
- Buna karsilik `Gereksiz yeni sistem veya katman onerme.` cizgisi korunacak; bu ifade skill'in validator, orchestrator, ek moduller veya gereksiz katmanlar onermeye kaymasini engelleyen aktif bir sinir olarak degerlendirildi.
- Pratik sonuc olarak Consultation Center karar/prompt talimatlarinda tekrar eden baglam cizgileri azaltilip, gercekten drift onleyen sinirlar acik constraint olarak tutulacak.

## Context policy ek notu
- Consultation prompt skill'ine baglam secimini kısıtlayici tablo yerine ilke bazli yoneten yeni referanslar eklendi: `references/context-injection-policy.md` ve `references/context-selection-matrix.md`.
- Runtime prompt loader `agent-workspace/lib/consultation-center/agent.ts` bu iki yeni referansi da zorunlu okuyacak sekilde genislendi; boylece prompt ureten agent artik once cekirdek baglam ozeti, sonra goreve gore ikincil kaynak secme cizgisiyle calisiyor.
- Build gecti ve skill yeniden paketlenip dogrulandi; yeni `.skill` paketi context policy referanslarini da iceriyor.
- Sonraki duzeltmede dosya-seti degerlendirme promptlarinin hala toplu rewrite moduna kayabildigi goruldu; skill ve referanslar `keep | trim | rewrite` karar cizgisini varsayilan hale getirecek sekilde guncellendi.
- Bu yeni cizgide dosya veya skill degerlendirme islerinde `finalContent` benzeri tam yeniden yazim alani sadece `trim` veya `rewrite` kararinda istenecek, `keep` kararinda zorlanmayacak.
- Dis GPT oturumlari icin yeni cizgi netlestirildi: repo analizi isteniyorsa repo icerigini incelemek zorunlu kabul edilecek; sadece promptta verilen kisa ozete bakip kesin repo analizi yapma cizgisi reddedildi.
- Skill ve referanslar, dis GPT tarafinda repo icerigi erisilebiliyorsa acma/inceleme, erisilemiyorsa bunu acik bosluk veya blokaj olarak yazma kuralina gore guncellendi.
- Sonraki duzeltmede bu cizgi clone-first olacak sekilde sertlestirildi: dis GPT repo analizi promptlarinda erisim mumkunse once repo klonlama, clone mumkun degilse esdeger repo incelemesi, bu da mumkun degilse acik blokaj yazma kuralina gecildi.
- Ardindan executor ayrimi netlestirildi: repo klonlama/işleme gorevi bizim agentin degil, final promptu okuyacak dis GPT'nin eylemi olarak yazilacak sekilde skill metni duzeltildi.
- Son kararda clone vurgusu tamamen geri alindi; dis GPT promptlarinda zorunlu olan sey klon degil, repo icerigini gercekten incelemek olarak netlestirildi.

## Aksam flush ek notu
- Admin uygulamasinda mock fallback temizleme cizgisi fiilen uygulandi: runtime artik Project OS ve Consultation Center tarafinda mock veriyle sessiz geri dusmek yerine veritabani bagimli calisacak sekilde ilerliyor.
- Bu turda ana admin yuzeyleri DB runtime davranisini korumak icin dynamic cizgiye alindi; build-time sahte veri yerine runtime Supabase verisi esas aliniyor.
- Deploy ve smoke testler tekrar gecti; `/api/project-os/*`, `/api/consultation-center`, `/project-os`, `/consultation-center` ve `/context-center` Supabase tabanli akista dogrulandi.
- Bu degisiklik workspace'te `e4e62a5` commit'i ile tutuldu: `Keep DB-backed admin pages dynamic`.
# 2026-04-22

## Gece ek notlar
- Prisma local bootstrap hatti tekrar kontrol edildi; `npm run prisma:bootstrap:local` komutu `.env` veya `.env.local` icinden `DATABASE_URL` okuyup `npx prisma migrate deploy` ve `npx prisma generate` akisini tek komutta kosacak sekilde hazir durumda.
- Dogrulama icin helper `bash -n` ile syntax kontrolunden gecirildi, `./bin/bootstrap-prisma-local --dry-run` beklenen ciktiyi verdi ve uygulama tarafinda `npm run build` tekrar gecti.
- Gercek DB apply hala bloklu; workspace icinde henuz kullanilabilir bir `DATABASE_URL` yok.

## Flush ek notu
- Prisma bootstrap yardimci hatti package script ve `.env.example` yonlendirmesiyle baglandi; calisma yolu `npm run prisma:bootstrap:local` olarak sabitlendi.
- Bu dogrulama ve kayit `e5c726f` commit'i ile tutuldu: `Record Prisma bootstrap verification`.

## Sabah ek notlar
- `npm run deploy:hesap-merkezi-v2` calistirildi; build gecti ve `esnafdijital-admin.service` yeni build ile restart edildi.
- Deploy script icindeki ilk verify denemesi restart sonrasi kisa bir gecikmede `Connection refused` aldi, fakat servis hemen sonra saglikli sekilde ayaga kalkti.
- Ardindan `./bin/verify-hesap-merkezi-v2` yeniden calistirildi ve `health`, `hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri basariyla gecti.
- Local root HTML yeni chunk referanslariyla donuyor ve bagli CSS asset'i `200 OK` veriyor; onceki stale HTML / missing CSS durumu kapandi.
- Admin menuden `Eski ekran > Profil araçları` girisi kaldirildi ve `/codex-profilleri` route'u silindi; build dogrulamasinda bu sayfa artik route listesinde yer almiyor.
- Bu kaldirma prod'a deploy edildi; servis restart sonrasi yeniden dogrulandi ve `/codex-profilleri` localde `404 Not Found` donmeye basladi.
- Consultation Center V1 sade prompt akisi once prompt/context kurallariyla guncellendi ve prod'a deploy edildi; hedef model secimi, otomatik prompt/context kurulumu ve sifir-hafiza + VPS/OpenClaw baglamli prompt uretimi canliya alindi.
- Bu deploy sonrasi `api/health`, `api/hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri yeniden gecti; restart sirasinda gorulen kisa `Connection refused` gecici startup anina aitti.

## Consultation Center agent trigger ek notu
- Consultation Center create akisi tekrar duzeltildi; yeni kayit artik promptu direkt gostermiyor, once `preparing` durumuyla aciliyor.
- Detail acilisinda `/api/consultation-center/[id]/suggest` agent hattini tetikliyor; agent brief’i zenginlestirdikten sonra prompt kayda yaziliyor ve durum `ready` oluyor, hata varsa `error` olarak gorunuyor.
- Inbox ve detail yuzeylerinde `preparing / ready / error` prompt durumu gorunur hale getirildi; smoke testte create sonrasi `promptStatus=preparing`, suggest sonrasi `promptStatus=ready` ve prompt uzunlugu `4405` karakter olarak dogrulandi.
- Bu duzeltme build ve deploy sonrasi dogrulandi; ilgili commit `eba990e`: `Record agent-triggered consultation prompt flow`.

## Consultation Center isolated agent ek notu
- Prompt hazirlama akisinda gorulen hata, admin uygulamasinin kendi icinden ayni `main` agent session hattini tekrar `openclaw agent` ile cagirmasindan kaynaklandi; hata izi `session file locked` ve ardindan `gateway timeout` olarak goruldu.
- Prompt hazirlama hattinda self-call yerine ayrik `esnafdijital` agent kullanimi sabitlendi; ayni session lock riski bu akistan cikarildi.
- Build ve deploy tekrar alindi; ardindan `consult_1776850238725` kaydi uzerinde suggest akisi yeniden calistirildi ve `promptStatus=ready`, `promptError=null`, `promptTextLength=2716` ile dogrulandi.
# 2026-04-22

## Gece ek notlar
- Prisma local bootstrap hatti tekrar kontrol edildi; `npm run prisma:bootstrap:local` komutu `.env` veya `.env.local` icinden `DATABASE_URL` okuyup `npx prisma migrate deploy` ve `npx prisma generate` akisini tek komutta kosacak sekilde hazir durumda.
- Dogrulama icin helper `bash -n` ile syntax kontrolunden gecirildi, `./bin/bootstrap-prisma-local --dry-run` beklenen ciktiyi verdi ve uygulama tarafinda `npm run build` tekrar gecti.
- Gercek DB apply hala bloklu; workspace icinde henuz kullanilabilir bir `DATABASE_URL` yok.

## Flush ek notu
- Prisma bootstrap yardimci hatti package script ve `.env.example` yonlendirmesiyle baglandi; calisma yolu `npm run prisma:bootstrap:local` olarak sabitlendi.
- Bu dogrulama ve kayit `e5c726f` commit'i ile tutuldu: `Record Prisma bootstrap verification`.

## Sabah ek notlar
- `npm run deploy:hesap-merkezi-v2` calistirildi; build gecti ve `esnafdijital-admin.service` yeni build ile restart edildi.
- Deploy script icindeki ilk verify denemesi restart sonrasi kisa bir gecikmede `Connection refused` aldi, fakat servis hemen sonra saglikli sekilde ayaga kalkti.
- Ardindan `./bin/verify-hesap-merkezi-v2` yeniden calistirildi ve `health`, `hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri basariyla gecti.
- Local root HTML yeni chunk referanslariyla donuyor ve bagli CSS asset'i `200 OK` veriyor; onceki stale HTML / missing CSS durumu kapandi.
- Admin menuden `Eski ekran > Profil araçları` girisi kaldirildi ve `/codex-profilleri` route'u silindi; build dogrulamasinda bu sayfa artik route listesinde yer almiyor.
- Bu kaldirma prod'a deploy edildi; servis restart sonrasi yeniden dogrulandi ve `/codex-profilleri` localde `404 Not Found` donmeye basladi.
- Consultation Center V1 sade prompt akisi once prompt/context kurallariyla guncellendi ve prod'a deploy edildi; hedef model secimi, otomatik prompt/context kurulumu ve sifir-hafiza + VPS/OpenClaw baglamli prompt uretimi canliya alindi.
- Bu deploy sonrasi `api/health`, `api/hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri yeniden gecti; restart sirasinda gorulen kisa `Connection refused` gecici startup anina aitti.

## Consultation Center agent trigger ek notu
- Consultation Center create akisi tekrar duzeltildi; yeni kayit artik promptu direkt gostermiyor, once `preparing` durumuyla aciliyor.
- Detail acilisinda `/api/consultation-center/[id]/suggest` agent hattini tetikliyor; agent brief’i zenginlestirdikten sonra prompt kayda yaziliyor ve durum `ready` oluyor, hata varsa `error` olarak gorunuyor.
- Inbox ve detail yuzeylerinde `preparing / ready / error` prompt durumu gorunur hale getirildi; smoke testte create sonrasi `promptStatus=preparing`, suggest sonrasi `promptStatus=ready` ve prompt uzunlugu `4405` karakter olarak dogrulandi.
- Bu duzeltme build ve deploy sonrasi dogrulandi; ilgili commit `eba990e`: `Record agent-triggered consultation prompt flow`.

## Consultation Center isolated agent ek notu
- Prompt hazirlama akisinda gorulen hata, admin uygulamasinin kendi icinden ayni `main` agent session hattini tekrar `openclaw agent` ile cagirmasindan kaynaklandi; hata izi `session file locked` ve ardindan `gateway timeout` olarak goruldu.
- Prompt hazirlama hattinda self-call yerine ayrik `esnafdijital` agent kullanimi sabitlendi; ayni session lock riski bu akistan cikarildi.
- Build ve deploy tekrar alindi; ardindan `consult_1776850238725` kaydi uzerinde suggest akisi yeniden calistirildi ve `promptStatus=ready`, `promptError=null`, `promptTextLength=2716` ile dogrulandi.

## Oglen oncesi flush ek notu
- Isolated agent duzeltmesi sonrasi mock guncelleme hattinda eski `promptError` degerinin basarili suggest sonrasi ustte asili kalabildigi goruldu; mock update mantigi temizlenerek basarili prompt hazirlamada hata alani `null` kalacak sekilde duzeltildi.
- Bu duzeltme ilk denemede TypeScript build hatasi verdi, ardindan duzeltilip yeniden build/deploy alindi; son verify kontrolleri tekrar gecti.
- Consultation Center prompt hazirlama akisi yeniden smoke test edildi ve kayit `promptStatus=ready`, `promptError=null`, `promptTextLength=2821` durumuyla dogrulandi.
- Prompt agent secimi kod icindeki sabit fallback'tan cikarildi; `CONSULTATION_PROMPT_AGENT_ID` env ayari zorunlu hale getirildi, `.env.example` belgelendi ve local runtime `.env.local` ile dogrulandi.
- GPT-5 Prompting Guide icin yerel calisma ozeti `REFERENCES/prompting/gpt5-prompting-principles.md` olarak eklendi; Consultation Center prompt ureten hat bu yerel referansi okuyup prompt olusturma agentina baglam olarak geciyor.
- Prompting referansi icin kod ici fallback kaldirildi; `REFERENCES/prompting/gpt5-prompting-principles.md` okunamaz veya bossa prompt hazirlama artik sessiz kalite dususu yerine acik hata veriyor.
- `skills/consultation-prompt-builder/` altinda yeni workspace skill olusturuldu; Consultation Center promptlarini consultation kaydindan uretme, duzeltme ve grounding kontrolu icin SKILL.md + iki reference dosyasi yazildi ve `consultation-prompt-builder.skill` olarak paket dogrulamasi gecti.
- App tarafindaki Consultation Center prompt hatti yeni skill'e baglandi; `agent-workspace/lib/consultation-center/agent.ts` artik `skills/consultation-prompt-builder/` altindaki SKILL ve reference dosyalarini dogrudan okuyup agent promptuna gomuyor. Build/deploy ve suggest smoke test yeniden gecti.
- Consultation prompt skill ve bagli app talimati tekrar duzeltildi; sifir-hafiza varsayimi artik ic kural olarak korunuyor ama final prompta meta cumle olarak yazilmiyor, bunun yerine gerekli baglam dogrudan prompta yerlestiriliyor. Ayrica teknik olmayan kisa ozet istegi tekrar zorunlu hale getirildi; smoke testte meta sifir-hafiza cumlesi cikmadi ve plain summary istegi promptta goruldu.
- Sorunun kokunu azaltmak icin aktif runtime ve skill dosyalarindan ornek meta cumle metni tamamen kaldirildi; agent talimati ve skill referanslari artik bu ifadeyi isim vererek anmiyor. Yeni suggest smoke testte ne tam ifade ne de oturum/baglam eksigi meta dili prompta sizmadi.
- Consultation prompt akisi bir adim daha sadeleştirildi: davranis/policy kurallari skill/reference tarafinda ana kaynak haline getirildi, `agent-workspace/lib/consultation-center/agent.ts` ise daha cok skill icerigini yukleyen ve uygulama JSON kontratini ileten ince bir loader katmanina indirildi. Build, deploy ve suggest smoke test tekrar gecti; prompt `ready` dondu ve rol + iki repo linki + teknik olmayan ozet istegi korunarak uretildi.
- `agent.ts` bir tur daha inceltildi; prompt icindeki aciklayici bootstrap ve uzun JSON sekil blogu kaldirildi, consultation payload secici hale getirildi ve bos context/brief bloklari artik prompta gereksiz dump edilmiyor. Build/deploy ve yeni gecici kayitla suggest smoke test tekrar gecti; prompt `ready` dondu, rol/repo linkleri/teknik olmayan ozet istegi korundu.
- Son inceltme turunda `agent.ts` promptundan bootstrap cumleleri ve prompt icindeki uygulama kontrati blogu da tamamen kaldirildi; skill/reference + kisa consultation payload hattina inildi. Build/deploy ve yeni gecici kayitla suggest smoke test yine gecti; prompt `ready` dondu ve rol, iki repo linki, teknik olmayan ozet istegi korunarak uretildi.
- Consultation prompt skill kurali tekrar netlestirildi: teknik olmayan kisa ozet artik JSON icinde `shortPlainSummary` benzeri alan olarak istenmiyor, final promptta JSON disinda ayri ikinci bolum olarak isteniyor. Yeni suggest smoke testte prompt `ready` dondu, JSON disi ozet dili goruldu ve `shortPlainSummary` izi kayboldu.
- Citation/footnote stili de skill seviyesinde yasaklandi; `consultation-prompt-builder` artik `[1]`, `[2]`, `([GitHub][1])` ve benzeri atif formatlarini reddediyor, link gerekiyorsa dogrudan URL istiyor. Yeni suggest smoke testte prompt `ready` dondu, citation izi gorulmedi ve iki repo linki duz URL olarak korundu.
- Consultation Center quick create bolumu acilip kapanabilir hale getirildi; ustteki istek metni karti artik kullanici isterse kapatilabiliyor. Build ve deploy tekrar gecti.
- Prompt hazir olduktan sonra `Degisiklik oner` akisi eklendi; kullanici promptta neyin degismesi gerektigini yazip ayni kayit uzerinden yeniden suggest tetikleyebiliyor. Suggest hattina `changeRequest` destegi eklendi, mevcut prompt bu istekle birlikte agente veriliyor ve son istek `sharedBrief.promptRevisionRequest` altinda tutuluyor. Build/deploy ve gecici kayitla smoke test gecti; ikinci suggest sonrasi prompt gercekten degisti ve kisaldi.
- Consultation prompt skill cizgisi tekrar netlestirildi: baglam secimi artik sayi siniri ile degil, istenen degisikligi guvenle kurmaya yetecek tum baglam mantigi ile tanimlaniyor. `contextRefs` limitinin sadece kaydedilen ana kaynak ozeti oldugu aciklandi, anti-pattern olarak baglami yapay sayi siniriyla daraltma eklendi ve grounding checklist bu yeni ilkeye gore guncellendi. Suggest smoke test yine gecti.
# 2026-04-22

## Flush ek notu
- Consultation Center prompt hattinda yeni kalici cizgi netlesti: validator once gelmeyecek; once prompt ciktisinin kendisi daraltilacak ve kok sorunlar prompt kaynaginda duzeltilecek.
- Kullanici, `Bu oturum sifir hafizali kabul edilsin` benzeri meta dilin semptom kontroluyle degil, kaynagindan kaldirilmasini istedi; bu nedenle runtime ve skill talimatlarinda meta-hafiza dilini tamamen anmama cizgisi benimsendi.
- Son smoke testler bu temizligi dogruladi: prompt `ready` dondu, tam ifade yoktu, bellek/oturum meta dili gorunmedi, teknik olmayan ozet istegi korundu.
- Bu turdaki ana commitler: `7e6344e` (`Bind consultation prompt flow to skill files`), `7dba523` (`Refine consultation prompt skill writing rules`), `39051f9` (`Remove consultation prompt memory-meta wording`).
- Dis degerlendirme icin pratik sonuc cikarildi: varsayimsal validator tartismasindan once `agent.ts` icinde kalan urun/yazim kurallarini skill/reference katmanina tasimak ve gercek consultation ornekleriyle golden sample testi hazirlamak daha dogru sonraki adim olarak goruluyor.
# 2026-04-22

## Gece ek notlar
- Prisma local bootstrap hatti tekrar kontrol edildi; `npm run prisma:bootstrap:local` komutu `.env` veya `.env.local` icinden `DATABASE_URL` okuyup `npx prisma migrate deploy` ve `npx prisma generate` akisini tek komutta kosacak sekilde hazir durumda.
- Dogrulama icin helper `bash -n` ile syntax kontrolunden gecirildi, `./bin/bootstrap-prisma-local --dry-run` beklenen ciktiyi verdi ve uygulama tarafinda `npm run build` tekrar gecti.
- Gercek DB apply hala bloklu; workspace icinde henuz kullanilabilir bir `DATABASE_URL` yok.

## Flush ek notu
- Prisma bootstrap yardimci hatti package script ve `.env.example` yonlendirmesiyle baglandi; calisma yolu `npm run prisma:bootstrap:local` olarak sabitlendi.
- Bu dogrulama ve kayit `e5c726f` commit'i ile tutuldu: `Record Prisma bootstrap verification`.

## Sabah ek notlar
- `npm run deploy:hesap-merkezi-v2` calistirildi; build gecti ve `esnafdijital-admin.service` yeni build ile restart edildi.
- Deploy script icindeki ilk verify denemesi restart sonrasi kisa bir gecikmede `Connection refused` aldi, fakat servis hemen sonra saglikli sekilde ayaga kalkti.
- Ardindan `./bin/verify-hesap-merkezi-v2` yeniden calistirildi ve `health`, `hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri basariyla gecti.
- Local root HTML yeni chunk referanslariyla donuyor ve bagli CSS asset'i `200 OK` veriyor; onceki stale HTML / missing CSS durumu kapandi.
- Admin menuden `Eski ekran > Profil araçları` girisi kaldirildi ve `/codex-profilleri` route'u silindi; build dogrulamasinda bu sayfa artik route listesinde yer almiyor.
- Bu kaldirma prod'a deploy edildi; servis restart sonrasi yeniden dogrulandi ve `/codex-profilleri` localde `404 Not Found` donmeye basladi.
- Consultation Center V1 sade prompt akisi once prompt/context kurallariyla guncellendi ve prod'a deploy edildi; hedef model secimi, otomatik prompt/context kurulumu ve sifir-hafiza + VPS/OpenClaw baglamli prompt uretimi canliya alindi.
- Bu deploy sonrasi `api/health`, `api/hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri yeniden gecti; restart sirasinda gorulen kisa `Connection refused` gecici startup anina aitti.

## Consultation Center agent trigger ek notu
- Consultation Center create akisi tekrar duzeltildi; yeni kayit artik promptu direkt gostermiyor, once `preparing` durumuyla aciliyor.
- Detail acilisinda `/api/consultation-center/[id]/suggest` agent hattini tetikliyor; agent brief’i zenginlestirdikten sonra prompt kayda yaziliyor ve durum `ready` oluyor, hata varsa `error` olarak gorunuyor.
- Inbox ve detail yuzeylerinde `preparing / ready / error` prompt durumu gorunur hale getirildi; smoke testte create sonrasi `promptStatus=preparing`, suggest sonrasi `promptStatus=ready` ve prompt uzunlugu `4405` karakter olarak dogrulandi.
- Bu duzeltme build ve deploy sonrasi dogrulandi; ilgili commit `eba990e`: `Record agent-triggered consultation prompt flow`.

## Consultation Center isolated agent ek notu
- Prompt hazirlama akisinda gorulen hata, admin uygulamasinin kendi icinden ayni `main` agent session hattini tekrar `openclaw agent` ile cagirmasindan kaynaklandi; hata izi `session file locked` ve ardindan `gateway timeout` olarak goruldu.
- Prompt hazirlama hattinda self-call yerine ayrik `esnafdijital` agent kullanimi sabitlendi; ayni session lock riski bu akistan cikarildi.
- Build ve deploy tekrar alindi; ardindan `consult_1776850238725` kaydi uzerinde suggest akisi yeniden calistirildi ve `promptStatus=ready`, `promptError=null`, `promptTextLength=2716` ile dogrulandi.
# 2026-04-22

## Gece ek notlar
- Prisma local bootstrap hatti tekrar kontrol edildi; `npm run prisma:bootstrap:local` komutu `.env` veya `.env.local` icinden `DATABASE_URL` okuyup `npx prisma migrate deploy` ve `npx prisma generate` akisini tek komutta kosacak sekilde hazir durumda.
- Dogrulama icin helper `bash -n` ile syntax kontrolunden gecirildi, `./bin/bootstrap-prisma-local --dry-run` beklenen ciktiyi verdi ve uygulama tarafinda `npm run build` tekrar gecti.
- Gercek DB apply hala bloklu; workspace icinde henuz kullanilabilir bir `DATABASE_URL` yok.

## Flush ek notu
- Prisma bootstrap yardimci hatti package script ve `.env.example` yonlendirmesiyle baglandi; calisma yolu `npm run prisma:bootstrap:local` olarak sabitlendi.
- Bu dogrulama ve kayit `e5c726f` commit'i ile tutuldu: `Record Prisma bootstrap verification`.

## Sabah ek notlar
- `npm run deploy:hesap-merkezi-v2` calistirildi; build gecti ve `esnafdijital-admin.service` yeni build ile restart edildi.
- Deploy script icindeki ilk verify denemesi restart sonrasi kisa bir gecikmede `Connection refused` aldi, fakat servis hemen sonra saglikli sekilde ayaga kalkti.
- Ardindan `./bin/verify-hesap-merkezi-v2` yeniden calistirildi ve `health`, `hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri basariyla gecti.
- Local root HTML yeni chunk referanslariyla donuyor ve bagli CSS asset'i `200 OK` veriyor; onceki stale HTML / missing CSS durumu kapandi.
- Admin menuden `Eski ekran > Profil araçları` girisi kaldirildi ve `/codex-profilleri` route'u silindi; build dogrulamasinda bu sayfa artik route listesinde yer almiyor.
- Bu kaldirma prod'a deploy edildi; servis restart sonrasi yeniden dogrulandi ve `/codex-profilleri` localde `404 Not Found` donmeye basladi.
- Consultation Center V1 sade prompt akisi once prompt/context kurallariyla guncellendi ve prod'a deploy edildi; hedef model secimi, otomatik prompt/context kurulumu ve sifir-hafiza + VPS/OpenClaw baglamli prompt uretimi canliya alindi.
- Bu deploy sonrasi `api/health`, `api/hesap-merkezi/status` ve `hesap-merkezi` sayfa kontrolleri yeniden gecti; restart sirasinda gorulen kisa `Connection refused` gecici startup anina aitti.

## Consultation Center agent trigger ek notu
- Consultation Center create akisi tekrar duzeltildi; yeni kayit artik promptu direkt gostermiyor, once `preparing` durumuyla aciliyor.
- Detail acilisinda `/api/consultation-center/[id]/suggest` agent hattini tetikliyor; agent brief’i zenginlestirdikten sonra prompt kayda yaziliyor ve durum `ready` oluyor, hata varsa `error` olarak gorunuyor.
- Inbox ve detail yuzeylerinde `preparing / ready / error` prompt durumu gorunur hale getirildi; smoke testte create sonrasi `promptStatus=preparing`, suggest sonrasi `promptStatus=ready` ve prompt uzunlugu `4405` karakter olarak dogrulandi.
- Bu duzeltme build ve deploy sonrasi dogrulandi; ilgili commit `eba990e`: `Record agent-triggered consultation prompt flow`.

## Consultation Center isolated agent ek notu
- Prompt hazirlama akisinda gorulen hata, admin uygulamasinin kendi icinden ayni `main` agent session hattini tekrar `openclaw agent` ile cagirmasindan kaynaklandi; hata izi `session file locked` ve ardindan `gateway timeout` olarak goruldu.
- Prompt hazirlama hattinda self-call yerine ayrik `esnafdijital` agent kullanimi sabitlendi; ayni session lock riski bu akistan cikarildi.
- Build ve deploy tekrar alindi; ardindan `consult_1776850238725` kaydi uzerinde suggest akisi yeniden calistirildi ve `promptStatus=ready`, `promptError=null`, `promptTextLength=2716` ile dogrulandi.

## Oglen oncesi flush ek notu
- Isolated agent duzeltmesi sonrasi mock guncelleme hattinda eski `promptError` degerinin basarili suggest sonrasi ustte asili kalabildigi goruldu; mock update mantigi temizlenerek basarili prompt hazirlamada hata alani `null` kalacak sekilde duzeltildi.
- Bu duzeltme ilk denemede TypeScript build hatasi verdi, ardindan duzeltilip yeniden build/deploy alindi; son verify kontrolleri tekrar gecti.
- Consultation Center prompt hazirlama akisi yeniden smoke test edildi ve kayit `promptStatus=ready`, `promptError=null`, `promptTextLength=2821` durumuyla dogrulandi.
- Prompt agent secimi kod icindeki sabit fallback'tan cikarildi; `CONSULTATION_PROMPT_AGENT_ID` env ayari zorunlu hale getirildi, `.env.example` belgelendi ve local runtime `.env.local` ile dogrulandi.
- GPT-5 Prompting Guide icin yerel calisma ozeti `REFERENCES/prompting/gpt5-prompting-principles.md` olarak eklendi; Consultation Center prompt ureten hat bu yerel referansi okuyup prompt olusturma agentina baglam olarak geciyor.
- Prompting referansi icin kod ici fallback kaldirildi; `REFERENCES/prompting/gpt5-prompting-principles.md` okunamaz veya bossa prompt hazirlama artik sessiz kalite dususu yerine acik hata veriyor.
- `skills/consultation-prompt-builder/` altinda yeni workspace skill olusturuldu; Consultation Center promptlarini consultation kaydindan uretme, duzeltme ve grounding kontrolu icin SKILL.md + iki reference dosyasi yazildi ve `consultation-prompt-builder.skill` olarak paket dogrulamasi gecti.
- App tarafindaki Consultation Center prompt hatti yeni skill'e baglandi; `agent-workspace/lib/consultation-center/agent.ts` artik `skills/consultation-prompt-builder/` altindaki SKILL ve reference dosyalarini dogrudan okuyup agent promptuna gomuyor. Build/deploy ve suggest smoke test yeniden gecti.
- Consultation prompt skill ve bagli app talimati tekrar duzeltildi; sifir-hafiza varsayimi artik ic kural olarak korunuyor ama final prompta meta cumle olarak yazilmiyor, bunun yerine gerekli baglam dogrudan prompta yerlestiriliyor. Ayrica teknik olmayan kisa ozet istegi tekrar zorunlu hale getirildi; smoke testte meta sifir-hafiza cumlesi cikmadi ve plain summary istegi promptta goruldu.
- Sorunun kokunu azaltmak icin aktif runtime ve skill dosyalarindan ornek meta cumle metni tamamen kaldirildi; agent talimati ve skill referanslari artik bu ifadeyi isim vererek anmiyor. Yeni suggest smoke testte ne tam ifade ne de oturum/baglam eksigi meta dili prompta sizmadi.
- Consultation prompt akisi bir adim daha sadeleştirildi: davranis/policy kurallari skill/reference tarafinda ana kaynak haline getirildi, `agent-workspace/lib/consultation-center/agent.ts` ise daha cok skill icerigini yukleyen ve uygulama JSON kontratini ileten ince bir loader katmanina indirildi. Build, deploy ve suggest smoke test tekrar gecti; prompt `ready` dondu ve rol + iki repo linki + teknik olmayan ozet istegi korunarak uretildi.
- `agent.ts` bir tur daha inceltildi; prompt icindeki aciklayici bootstrap ve uzun JSON sekil blogu kaldirildi, consultation payload secici hale getirildi ve bos context/brief bloklari artik prompta gereksiz dump edilmiyor. Build/deploy ve yeni gecici kayitla suggest smoke test tekrar gecti; prompt `ready` dondu, rol/repo linkleri/teknik olmayan ozet istegi korundu.
- Son inceltme turunda `agent.ts` promptundan bootstrap cumleleri ve prompt icindeki uygulama kontrati blogu da tamamen kaldirildi; skill/reference + kisa consultation payload hattina inildi. Build/deploy ve yeni gecici kayitla suggest smoke test yine gecti; prompt `ready` dondu ve rol, iki repo linki, teknik olmayan ozet istegi korunarak uretildi.
- Consultation prompt skill kurali tekrar netlestirildi: teknik olmayan kisa ozet artik JSON icinde `shortPlainSummary` benzeri alan olarak istenmiyor, final promptta JSON disinda ayri ikinci bolum olarak isteniyor. Yeni suggest smoke testte prompt `ready` dondu, JSON disi ozet dili goruldu ve `shortPlainSummary` izi kayboldu.
- Citation/footnote stili de skill seviyesinde yasaklandi; `consultation-prompt-builder` artik `[1]`, `[2]`, `([GitHub][1])` ve benzeri atif formatlarini reddediyor, link gerekiyorsa dogrudan URL istiyor. Yeni suggest smoke testte prompt `ready` dondu, citation izi gorulmedi ve iki repo linki duz URL olarak korundu.
- Consultation Center quick create bolumu acilip kapanabilir hale getirildi; ustteki istek metni karti artik kullanici isterse kapatilabiliyor. Build ve deploy tekrar gecti.
- Prompt hazir olduktan sonra `Degisiklik oner` akisi eklendi; kullanici promptta neyin degismesi gerektigini yazip ayni kayit uzerinden yeniden suggest tetikleyebiliyor. Suggest hattina `changeRequest` destegi eklendi, mevcut prompt bu istekle birlikte agente veriliyor ve son istek `sharedBrief.promptRevisionRequest` altinda tutuluyor. Build/deploy ve gecici kayitla smoke test gecti; ikinci suggest sonrasi prompt gercekten degisti ve kisaldi.
- Consultation prompt skill cizgisi tekrar netlestirildi: baglam secimi artik sayi siniri ile degil, istenen degisikligi guvenle kurmaya yetecek tum baglam mantigi ile tanimlaniyor. `contextRefs` limitinin sadece kaydedilen ana kaynak ozeti oldugu aciklandi, anti-pattern olarak baglami yapay sayi siniriyla daraltma eklendi ve grounding checklist bu yeni ilkeye gore guncellendi. Suggest smoke test yine gecti.
# 2026-04-22

## Flush ek notu
- Consultation Center prompt hattinda yeni kalici cizgi netlesti: validator once gelmeyecek; once prompt ciktisinin kendisi daraltilacak ve kok sorunlar prompt kaynaginda duzeltilecek.
- Kullanici, `Bu oturum sifir hafizali kabul edilsin` benzeri meta dilin semptom kontroluyle degil, kaynagindan kaldirilmasini istedi; bu nedenle runtime ve skill talimatlarinda meta-hafiza dilini tamamen anmama cizgisi benimsendi.
- Son smoke testler bu temizligi dogruladi: prompt `ready` dondu, tam ifade yoktu, bellek/oturum meta dili gorunmedi, teknik olmayan ozet istegi korundu.
- Bu turdaki ana commitler: `7e6344e` (`Bind consultation prompt flow to skill files`), `7dba523` (`Refine consultation prompt skill writing rules`), `39051f9` (`Remove consultation prompt memory-meta wording`).
- Dis degerlendirme icin pratik sonuc cikarildi: varsayimsal validator tartismasindan once `agent.ts` icinde kalan urun/yazim kurallarini skill/reference katmanina tasimak ve gercek consultation ornekleriyle golden sample testi hazirlamak daha dogru sonraki adim olarak goruluyor.

## Ogleden sonra flush ek notu
- Consultation prompt skill'i icin sinir yazimi netlestirildi: `Kucuk isletme, sade MVP ve dusuk operasyon yuku cizgisini koru.` ifadesi ayri bir zorunlu constraint olarak tutulmak zorunda degil, cunku proje baglaminda zaten tasiniyor.
- Buna karsilik `Gereksiz yeni sistem veya katman onerme.` cizgisi korunacak; bu ifade skill'in validator, orchestrator, ek moduller veya gereksiz katmanlar onermeye kaymasini engelleyen aktif bir sinir olarak degerlendirildi.
- Pratik sonuc olarak Consultation Center karar/prompt talimatlarinda tekrar eden baglam cizgileri azaltilip, gercekten drift onleyen sinirlar acik constraint olarak tutulacak.

## Context policy ek notu
- Consultation prompt skill'ine baglam secimini kısıtlayici tablo yerine ilke bazli yoneten yeni referanslar eklendi: `references/context-injection-policy.md` ve `references/context-selection-matrix.md`.
- Runtime prompt loader `agent-workspace/lib/consultation-center/agent.ts` bu iki yeni referansi da zorunlu okuyacak sekilde genislendi; boylece prompt ureten agent artik once cekirdek baglam ozeti, sonra goreve gore ikincil kaynak secme cizgisiyle calisiyor.
- Build gecti ve skill yeniden paketlenip dogrulandi; yeni `.skill` paketi context policy referanslarini da iceriyor.
- Sonraki duzeltmede dosya-seti degerlendirme promptlarinin hala toplu rewrite moduna kayabildigi goruldu; skill ve referanslar `keep | trim | rewrite` karar cizgisini varsayilan hale getirecek sekilde guncellendi.
- Bu yeni cizgide dosya veya skill degerlendirme islerinde `finalContent` benzeri tam yeniden yazim alani sadece `trim` veya `rewrite` kararinda istenecek, `keep` kararinda zorlanmayacak.
- Dis GPT oturumlari icin yeni cizgi netlestirildi: repo analizi isteniyorsa repo icerigini incelemek zorunlu kabul edilecek; sadece promptta verilen kisa ozete bakip kesin repo analizi yapma cizgisi reddedildi.
- Skill ve referanslar, dis GPT tarafinda repo icerigi erisilebiliyorsa acma/inceleme, erisilemiyorsa bunu acik bosluk veya blokaj olarak yazma kuralina gore guncellendi.
- Sonraki duzeltmede bu cizgi clone-first olacak sekilde sertlestirildi: dis GPT repo analizi promptlarinda erisim mumkunse once repo klonlama, clone mumkun degilse esdeger repo incelemesi, bu da mumkun degilse acik blokaj yazma kuralina gecildi.
- Ardindan executor ayrimi netlestirildi: repo klonlama/işleme gorevi bizim agentin degil, final promptu okuyacak dis GPT'nin eylemi olarak yazilacak sekilde skill metni duzeltildi.
- Son kararda clone vurgusu tamamen geri alindi; dis GPT promptlarinda zorunlu olan sey klon degil, repo icerigini gercekten incelemek olarak netlestirildi.

## Aksam flush ek notu
- Admin uygulamasinda mock fallback temizleme cizgisi fiilen uygulandi: runtime artik Project OS ve Consultation Center tarafinda mock veriyle sessiz geri dusmek yerine veritabani bagimli calisacak sekilde ilerliyor.
- Bu turda ana admin yuzeyleri DB runtime davranisini korumak icin dynamic cizgiye alindi; build-time sahte veri yerine runtime Supabase verisi esas aliniyor.
- Deploy ve smoke testler tekrar gecti; `/api/project-os/*`, `/api/consultation-center`, `/project-os`, `/consultation-center` ve `/context-center` Supabase tabanli akista dogrulandi.
- Bu degisiklik workspace'te `e4e62a5` commit'i ile tutuldu: `Keep DB-backed admin pages dynamic`.

## Gece flush ek notu
- Project OS icin kullaniciya net cizgi tekrar anlatildi: burasi genel CRM degil, isletme -> audit -> teklif -> teslimat -> bakim hattinda "simdi ne hareket etmeli" sorusunu gosteren operasyon masasi.
- Project OS kuyrugunda status ilerletme sonrasi ilgili business'i ustte odakta tutan ve guncellenen satiri/banneri gosteren hafif UX iyilestirmesi canliya alindi; gercek pilot audit ileri-geri patch smoke testiyle dogrulandi. Commit: `2af3e3b` (`Highlight updated Project OS records after advance`).
- Discovery hattinda sonraki scrape'ler icin sahiplik sinyali kalici hale getirildi: Apify `claimThisBusiness` alani summary pipeline'a tasindi, turetilmis `ownershipStatus = claimed | unclaimed | unknown` eklendi ve Discovery ekranina `Sahiplik` kolonu + bu alana gore siralama getirildi. Commit: `f749090` (`Track discovery ownership status`).
- Project OS teklif kaydi gercek operasyon girdisine biraz daha yaklastirildi: offer modeline `addonKeys`, `domainPreference` ve `customDomain` alanlari eklendi, Prisma migration Supabase'a uygulandi.
- Offer create formu artik opsiyonel ekleri ve domain tercihini topluyor; delivery create formu ise secili isletmenin son teklif kaydindan otomatik `scope taslagi` oneriyor.
- Build, deploy ve reversible smoke test gecti; mevcut pilot offer gecici olarak `ozel domain + ek yorum materyali` ile patch edilip `/project-os` uzerinde domain, ekler ve `Scope taslagini doldur` akisinin gorundugu dogrulandi, sonra kayit eski haline alindi.
- Discovery adaylarindan 3 gercek pilot zincir acildi: `Berber Serkan` -> `Paket 1 + ek kartvizit`, `Efsane Kuaför` -> `Paket 2 + ek yorum materyali`, `Salon Tıraşizm` -> `Paket 3 + ozel domain tercihi`.
- Bu pilotlardan sonra opsiyonel ek cizgisi netlestirildi: `ozel domain` tum paketlerde ayri ek kalacak, `ek kartvizit` Paket 1 icindeki tek kartvizit tasariminin disinda ikinci varyasyon oldugunda acilacak, `ek yorum materyali` Paket 2-3 icindeki temel yorum akisinin ustune ek fiziksel materyal gerektiginde acilacak, `kucuk sosyal medya duzenleme` ise Paket 1-2 tarafinda hafif profil dokunusu icin kullanilacak; Paket 3 icinde temel Instagram kurulumu zaten paket ici sayilacak.
- Offer addon aciklamalari ve `OFFERS.md` bu operasyon kararina gore guncellendi; build/deploy ve `/project-os` smoke testinde yeni pilot isimleri ile addon etiketleri gorunur sekilde dogrulandi.
- HEARTBEAT'teki delivery scope testi de bir adim ilerletildi: `buildDeliveryScopeSuggestion` asset, erisim, kickoff sonrasi operasyon ve yayin oncesi kontrol basliklariyla genisletildi.
- Bu testte `Salon Tıraşizm` kaydi delivery kickoff'a tasindi; ilgili offer kaydindaki `custom_domain` okuma bug'i duzeltildi ve Project OS API/UI tarafinda `custom-domain` dogru gorunur hale geldi.
- Build/deploy ve smoke test gecti; `/project-os` uzerinde `Gerekli assetler`, `Gerekli erisimler`, `Kickoff sonrasi operasyon adimlari` goruldu ve yeni delivery kaydinda bu checklist bloklari kalici scope metni olarak yer aldi.
- Ikinci pilot testi de yapildi: `Efsane Kuaför` icin delivery kickoff acildi ve ayni checklist yapisi asset, erisim, opsiyonel ek, kickoff ve yayin kontrol basliklariyla yeterli netlik verdi.
- Bu iki pilot sonrasi kalici karar acildi: delivery kickoff checklist'i MVP'de ayri operator alanlarina bolunmeyecek, tek `scope` metni icinde yari-yapili baslikli formatta kalacak. Ayri alanlara gecis ancak owner/status/due-date ihtiyaci dogarsa tekrar acilacak. Karar dosyasi: `DECISIONS/2026-04-22-delivery-scope-text-first.md`.
- Kurucu urun yonunde yeni karar verdi: panel dar operasyon masasi olarak kalmayacak, EsnafDigital icin genisleyen operasyonel CRM yonunde ilerleyecek.
- Buna gore `genel CRM` reddi yatay SaaS urunu anlamina cekildi; business detail, activity timeline, ic not, next step ve gorev takibi gibi CRM yuzeyleri panel kapsamina alinabilir hale geldi. Kalici karar dosyasi: `DECISIONS/2026-04-22-crm-expansion-direction.md`.
