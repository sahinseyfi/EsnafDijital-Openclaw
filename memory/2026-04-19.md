# 2026-04-19

- Codex auth profile mimarisinde clone/workspace-id yaklaşımından canonical native kimlik çizgisine geçiş başlatıldı.
- Yeni native kimlik kuralı: `principalKey + accountId`; canonical profile id formatı `openai-codex:<principalKey>__acct_<accountId>`.
- Auth completion sonrası ham/raw profile artık yeni workspace clone'u üretmek yerine canonical profile id'ye taşınıyor; eski raw id için auth-store ve panel artifact temizliği yapılıyor.
- Eski `profile-create` route'u kapatıldı; ayrı profil açma yolu artık yeniden auth yapıp gerçek hesap bağlamını canonical olarak kaydetme çizgisine çekildi.
- Canlı mevcut profil de canonical id'ye taşındı: `openai-codex:sahinseyfi@gmail.com__acct_0a7386cb-6117-41d4-875d-3b4cc70e0692`.
- Canonical auth geçişinin workspace commit'i: `b7af5e8` (`Canonicalize codex auth profile identities`).
- Kullanımda görüldü ki strict canonical yaklaşım teknik olarak temiz olsa da operatörün ayrı satır beklentisini tek başına karşılamıyor; bu yüzden eski ekrandan bağımsız yeni bir V2 rota başlatıldı.
- Yeni rota `/hesap-merkezi`; bu ekran sadece gerçek auth kaydı, current profil, operator görünen adı ve teknik kimlik (`accountId`, `profileId`) ilişkisini net göstermek için kuruldu.
- V2 için ayrı bağlam dosyası `openclaw-control-ui/ACCOUNT_CENTER_V2.md` eklendi; amaç eski `/codex-profilleri` ekranının kavramsal yükünü taşımadan yeni sistemi sade şekilde inşa etmek.
- `/hesap-merkezi` V2 içine ilk gerçek aksiyonlar bağlandı: yeni auth başlatma, callback ile kaydetme, current switch, ad/not güncelleme ve profil silme.
- V2 tarafında kendi API yüzeyi açıldı: `/api/hesap-merkezi/status`, `/api/hesap-merkezi/switch`, `/api/hesap-merkezi/meta`, `/api/hesap-merkezi/delete`.
- Canlı doğrulamada yeni V2 current-switch akışı çalıştı; profil geçişi `ChatGptBusiness` -> `Workspace1` olarak test edilip geri alındı.
- `/hesap-merkezi` auth akışı da eski `/api/auth-session*` yüzeyinden ayrılıp kendi V2 route'larına taşındı: `/api/hesap-merkezi/auth/start`, `/submit`, `/status`, `/cancel`.
- Heartbeat runtime'ında elevated kapalı olduğu için bu geliştirme turunda canlı restart/doğrulama yetki kapısına takıldı; kod ve build tamamlandı, deploy doğrulaması normal oturumda yapılmalı.
