# 2026-04-18

- `admin.esnafdijital.com.tr` artık basic auth arkasında çalışan gerçek Next.js admin uygulamasına yönleniyor; ana operator ekranı `/codex-profilleri`.
- Admin giriş bilgileri canlı yapılandırmada `mavizebra / 116281339Ss-` olarak kullanıldı.
- Admin uygulama kaynak yolu `/root/.openclaw/workspace/openclaw-control-ui`, systemd servis dosyası `esnafdijital-admin.service`, uygulama iç portu `3012`.
- `/codex-profilleri` ekranı gerçek OpenClaw `openai-codex` auth akışına bağlandı: auth link üretme, callback/code yapıştırma, profil listeleme, current session switch, global auth order switch, profil notu/gizleme, gateway restart.
- Bu ekran için proje içine iki yardımcı script eklendi: `openclaw-control-ui/bin/esnafdijital-openclaw-auth` ve `openclaw-control-ui/bin/esnafdijital-openclaw-discovery`.
- Kalıcı panel overlay/state dosyası `/opt/esnafdijital/data/codex-dashboard-overlay.json` olarak kullanılıyor.
- Bu teslimatın workspace commit'i: `c5fd33f` (`Launch codex profiles admin dashboard`).
# 2026-04-18

- `admin.esnafdijital.com.tr` artık basic auth arkasında çalışan gerçek Next.js admin uygulamasına yönleniyor; ana operator ekranı `/codex-profilleri`.
- Admin giriş bilgileri canlı yapılandırmada `mavizebra / 116281339Ss-` olarak kullanıldı.
- Admin uygulama kaynak yolu `/root/.openclaw/workspace/openclaw-control-ui`, systemd servis dosyası `esnafdijital-admin.service`, uygulama iç portu `3012`.
- `/codex-profilleri` ekranı gerçek OpenClaw `openai-codex` auth akışına bağlandı: auth link üretme, callback/code yapıştırma, profil listeleme, current session switch, global auth order switch, profil notu/gizleme, gateway restart.
- Bu ekran için proje içine iki yardımcı script eklendi: `openclaw-control-ui/bin/esnafdijital-openclaw-auth` ve `openclaw-control-ui/bin/esnafdijital-openclaw-discovery`.
- Kalıcı panel overlay/state dosyası `/opt/esnafdijital/data/codex-dashboard-overlay.json` olarak kullanılıyor.
- Bu teslimatın workspace commit'i: `c5fd33f` (`Launch codex profiles admin dashboard`).
- Auth helper tarafında PTY `EIO` yüzünden tamamlanıp panelde `verifying` takılı kalan sessionlar için self-heal mantığı eklendi; terminal durum artık log/result üzerinden toparlanıyor. Workspace commit'i: `958b9cb` (`Fix stuck codex auth session completion`).
- Aynı OpenAI hesabıyla yeniden auth olunduğunda yeni satır açılmayabileceği kabul edildi; panelde auth sonrası `displayName` ve `note` mevcut profile uygulanıyor ve current profile görünürlüğü artırıldı. Workspace commit'i: `a857aeb` (`Improve codex profile save visibility`).
- Kullanıcının gerçek ihtiyacı, aynı email altındaki farklı ChatGPT Business workspace'leri sadece etiketli alias olarak değil ayrı auth slotları gibi yönetmekti.
- Bu ihtiyaç için `openclaw-control-ui/lib/codex-dashboard/auth-store.ts` eklendi; agent auth store içindeki bir gerçek profile klonlanarak workspace'e özel yeni profile id üretilebiliyor.
- Workspace'e özel auth profile id formatı `openai-codex:<email>__ws_<slug>` çizgisinde tutuluyor; böylece aynı email altında birden fazla ayrı auth profile panelde gerçek auth profile olarak görünebiliyor.
# 2026-04-18

- `admin.esnafdijital.com.tr` artık basic auth arkasında çalışan gerçek Next.js admin uygulamasına yönleniyor; ana operator ekranı `/codex-profilleri`.
- Admin giriş bilgileri canlı yapılandırmada `mavizebra / 116281339Ss-` olarak kullanıldı.
- Admin uygulama kaynak yolu `/root/.openclaw/workspace/openclaw-control-ui`, systemd servis dosyası `esnafdijital-admin.service`, uygulama iç portu `3012`.
- `/codex-profilleri` ekranı gerçek OpenClaw `openai-codex` auth akışına bağlandı: auth link üretme, callback/code yapıştırma, profil listeleme, current session switch, global auth order switch, profil notu/gizleme, gateway restart.
- Bu ekran için proje içine iki yardımcı script eklendi: `openclaw-control-ui/bin/esnafdijital-openclaw-auth` ve `openclaw-control-ui/bin/esnafdijital-openclaw-discovery`.
- Kalıcı panel overlay/state dosyası `/opt/esnafdijital/data/codex-dashboard-overlay.json` olarak kullanılıyor.
- Bu teslimatın workspace commit'i: `c5fd33f` (`Launch codex profiles admin dashboard`).
# 2026-04-18

- `admin.esnafdijital.com.tr` artık basic auth arkasında çalışan gerçek Next.js admin uygulamasına yönleniyor; ana operator ekranı `/codex-profilleri`.
- Admin giriş bilgileri canlı yapılandırmada `mavizebra / 116281339Ss-` olarak kullanıldı.
- Admin uygulama kaynak yolu `/root/.openclaw/workspace/openclaw-control-ui`, systemd servis dosyası `esnafdijital-admin.service`, uygulama iç portu `3012`.
- `/codex-profilleri` ekranı gerçek OpenClaw `openai-codex` auth akışına bağlandı: auth link üretme, callback/code yapıştırma, profil listeleme, current session switch, global auth order switch, profil notu/gizleme, gateway restart.
- Bu ekran için proje içine iki yardımcı script eklendi: `openclaw-control-ui/bin/esnafdijital-openclaw-auth` ve `openclaw-control-ui/bin/esnafdijital-openclaw-discovery`.
- Kalıcı panel overlay/state dosyası `/opt/esnafdijital/data/codex-dashboard-overlay.json` olarak kullanılıyor.
- Bu teslimatın workspace commit'i: `c5fd33f` (`Launch codex profiles admin dashboard`).
- Auth helper tarafında PTY `EIO` yüzünden tamamlanıp panelde `verifying` takılı kalan sessionlar için self-heal mantığı eklendi; terminal durum artık log/result üzerinden toparlanıyor. Workspace commit'i: `958b9cb` (`Fix stuck codex auth session completion`).
- Aynı OpenAI hesabıyla yeniden auth olunduğunda yeni satır açılmayabileceği kabul edildi; panelde auth sonrası `displayName` ve `note` mevcut profile uygulanıyor ve current profile görünürlüğü artırıldı. Workspace commit'i: `a857aeb` (`Improve codex profile save visibility`).
- Kullanıcının gerçek ihtiyacı, aynı email altındaki farklı ChatGPT Business workspace'leri sadece etiketli alias olarak değil ayrı auth slotları gibi yönetmekti.
- Bu ihtiyaç için `openclaw-control-ui/lib/codex-dashboard/auth-store.ts` eklendi; agent auth store içindeki bir gerçek profile klonlanarak workspace'e özel yeni profile id üretilebiliyor.
- Workspace'e özel auth profile id formatı `openai-codex:<email>__ws_<slug>` çizgisinde tutuluyor; böylece aynı email altında birden fazla ayrı auth profile panelde gerçek auth profile olarak görünebiliyor.
- OpenClaw source incelemesinde `openai-codex` auth tarafının pratikte `chatgpt_account_id` ve `chatgpt-account-id` üstünden çalıştığı, birinci sınıf workspace/business-auth modeli göstermediği görüldü.
- Bu yüzden ileriye dönük temiz mimari kararı olarak şu çizgi benimsendi: gerçek auth profile, gerçek credential/account'ı temsil eder; workspace/business ayrımı ise varsayılan olarak operator metadata/context katmanında modellenmelidir.
- Aynı email altında birden fazla workspace ihtiyacı için ayrı auth id klonlama ancak OpenClaw upstream'de gerçek workspace-scoped auth kanıtı yoksa native model değil, yerel operator katmanı çözümü olarak değerlendirilmelidir.
# 2026-04-18

- OpenClaw tarafında gerçek `openai-codex` kullanım verisi `models status` çıktısından değil provider-usage hattından geliyor; çekirdek fetcher `fetchCodexUsage(...)` ve kaynak uç `https://chatgpt.com/backend-api/wham/usage`.
- Dashboard için doğru veri modeli `ProviderUsageSnapshot.windows` oldu; pratikte `5h` ve `Week` pencereleri `usedPercent` ve `resetAt` ile okunuyor.
- Bu yüzden `/codex-profilleri` tarafında usage gösterilecekse sezgisel/tahmini değer değil, OpenClaw provider-usage pipeline veya onu saran yerel helper üzerinden beslenmesi gerekiyor.
