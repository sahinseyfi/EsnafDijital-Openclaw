# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.

## Plugin ve scrape arastirmasi
- Scrape tarafini guclendirebilecek bundled adaylar `firecrawl`, `exa`, `browser`, `duckduckgo`, `brave` olarak kontrol edildi; bu ortamda allowlist disinda kaldiklari icin aktif kullanima gecmedi.
- Kisa maliyet sonucu: `Firecrawl` tek seferlik sinirli free kredi ile basliyor, `Exa` esasen ucretli, `Brave` API key gerektiriyor; bu yuzden yakin vadede ucretsiz/guvenilir omurga `internal website check + mevcut web arastirmasi + Apify` cizgisinde kaldi.

## Agent browser kurulumu
- Supheli gorunen browser skill adaylari elendi; temiz aday olarak `agent-browser-core` secildi ve workspace'e kuruldu. Commit: `52d1941` (`Install agent-browser-core skill`).
- Alt runtime da kuruldu: `agent-browser@0.26.0` global npm ile yuklendi, `agent-browser install` ile Chrome binary indirildi, Linux bagimlilik eksigi nedeniyle `agent-browser install --with-deps` calistirildi.
- Smoke test gecti: `agent-browser open https://example.com --session smoke-test` ve `agent-browser get title --session smoke-test` calisti, sonra session kapatildi.
- Host tarafinda ek not: kurulum sonunda kernel upgrade uyarisi geldi (`6.8.0-110-generic` bekleniyor); browser runtime icin engel degil ama host reboot planlanabilir.

## Business detail referans birlestirme
- `REFERENCES/businesses-page-capability-breakdown.md`, `REFERENCES/business-detail-data-contract-v1.md` ve `REFERENCES/businesses-page-plan.md` tek kanonik dokumanda birlestirildi: `REFERENCES/business-detail-v1.md`.
- Yeni dokuman business detail sayfasinin rolunu, veri katmanlarini, UX akisini, scrape paneli cizgisini ve V1/V2 acilis sirasini tek yerde topluyor.
- Eski uc dosya silinmedi; kirik referans olusmamasi icin `deprecated` yonlendirme notuna indirildi.

## Refresh timeline baglama
- Business detail activity timeline'a dis veri yenileme eventleri eklendi; sayfa artik business'e bagli refresh snapshot gecmisini okuyup `Dis veri snapshot yenilendi` girdileri uretiyor.
- Timeline metni arama kapsami, yorum/puan sinyali ve website var/yok ozeti tasiyor; boylece manuel `Detayli isletme verilerini al` aksiyonunun sonucu ayni kayitta gorunur oldu.
- `HEARTBEAT.md` siradaki somut adimi buna gore guncellendi: kalan is gercek kayitta canli smoke test yapmak.

## Canli smoke test notu
- `Berber Serkan` kaydinda guncel build ayri portta kaldirilarak smoke test ilerletildi; snapshot karti `23 Nis 02:08` verisini ve timeline `Dis veri snapshot yenilendi` eventini gercek kayitta gosterdi.
- Manuel yenileme API route'u ise Next runtime icinde halen `500` donuyor; disaridan ayni Apify komutu kosabildigi halde route icindeki cagri sessiz sekilde fail oluyor.
- Route catch blogu stdout/stderr detayi donecek sekilde sertlestirildi ama hata halen bos geliyor; siradaki teknik is runtime ile shell arasindaki bu farki daraltmak.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.

## Plugin ve scrape arastirmasi
- Scrape tarafini guclendirebilecek bundled adaylar `firecrawl`, `exa`, `browser`, `duckduckgo`, `brave` olarak kontrol edildi; bu ortamda allowlist disinda kaldiklari icin aktif kullanima gecmedi.
- Kisa maliyet sonucu: `Firecrawl` tek seferlik sinirli free kredi ile basliyor, `Exa` esasen ucretli, `Brave` API key gerektiriyor; bu yuzden yakin vadede ucretsiz/guvenilir omurga `internal website check + mevcut web arastirmasi + Apify` cizgisinde kaldi.

## Agent browser kurulumu
- Supheli gorunen browser skill adaylari elendi; temiz aday olarak `agent-browser-core` secildi ve workspace'e kuruldu. Commit: `52d1941` (`Install agent-browser-core skill`).
- Alt runtime da kuruldu: `agent-browser@0.26.0` global npm ile yuklendi, `agent-browser install` ile Chrome binary indirildi, Linux bagimlilik eksigi nedeniyle `agent-browser install --with-deps` calistirildi.
- Smoke test gecti: `agent-browser open https://example.com --session smoke-test` ve `agent-browser get title --session smoke-test` calisti, sonra session kapatildi.
- Host tarafinda ek not: kurulum sonunda kernel upgrade uyarisi geldi (`6.8.0-110-generic` bekleniyor); browser runtime icin engel degil ama host reboot planlanabilir.

## Business detail referans birlestirme
- `REFERENCES/businesses-page-capability-breakdown.md`, `REFERENCES/business-detail-data-contract-v1.md` ve `REFERENCES/businesses-page-plan.md` tek kanonik dokumanda birlestirildi: `REFERENCES/business-detail-v1.md`.
- Yeni dokuman business detail sayfasinin rolunu, veri katmanlarini, UX akisini, scrape paneli cizgisini ve V1/V2 acilis sirasini tek yerde topluyor.
- Eski uc dosya silinmedi; kirik referans olusmamasi icin `deprecated` yonlendirme notuna indirildi.

## Refresh timeline baglama
- Business detail activity timeline'a dis veri yenileme eventleri eklendi; sayfa artik business'e bagli refresh snapshot gecmisini okuyup `Dis veri snapshot yenilendi` girdileri uretiyor.
- Timeline metni arama kapsami, yorum/puan sinyali ve website var/yok ozeti tasiyor; boylece manuel `Detayli isletme verilerini al` aksiyonunun sonucu ayni kayitta gorunur oldu.
- `HEARTBEAT.md` siradaki somut adimi buna gore guncellendi: kalan is gercek kayitta canli smoke test yapmak.

## Canli smoke test notu
- `Berber Serkan` kaydinda guncel build ayri portta kaldirilarak smoke test ilerletildi; snapshot karti `23 Nis 02:08` verisini ve timeline `Dis veri snapshot yenilendi` eventini gercek kayitta gosterdi.
- Manuel yenileme API route'u ise Next runtime icinde halen `500` donuyor; disaridan ayni Apify komutu kosabildigi halde route icindeki cagri sessiz sekilde fail oluyor.
- Route catch blogu stdout/stderr detayi donecek sekilde sertlestirildi ama hata halen bos geliyor; siradaki teknik is runtime ile shell arasindaki bu farki daraltmak.

## Deep research oncelik listesi
- En degerli arastirma basliklari su sirada netlesti: `teklif omurgasi V1`, `Business Detail vs Project OS siniri`, `hafif tarama / derin tarama urun kontrati`, `audit snapshot veri modeli`, `operasyonel CRM ama yatay SaaS degil`, `Hesap Merkezi V2 bilgi mimarisi`, `delivery scope standardi`.
- Ikinci halka arastirma basliklari: `ilk 3 segment icin teklif-persona farklari`, `kanonik isletme kaydi yasam dongusu`, `Consultation Center karar hatti`.

## Refresh route kapanis notu
- `refresh-discovery` 500'unun asil nedeni Apify komutunun kendisi degil, route icindeki `execFile` bekleme hattiydi; saf shell ayni komutu `41s` civarinda tamamlarken `execFile` child pipe'larini acik tuttugu icin timeout'a gidiyordu.
- Route `spawn + stdio ignore` cizgisine alindi ve timeout `300s` olarak korundu; build tekrar gecti.
- Izole runtime smoke testinde `Berber Serkan` kaydina POST `/api/businesses/:id/refresh-discovery` `200` dondu, yeni snapshot yazildi ve Business Detail sayfasinda `23 Nis 03:00` snapshot zamani ile timeline girdisi gorundu.
- Ardindan ana runtime deploy edildi, `3012` uzerinde ayni endpoint tekrar `200` verdi ve blocker kapandi.

## Hazirlik / Tarama panel dokumani
- `REFERENCES/business-detail-v1.md` icindeki `Hazirlik / Tarama paneli` bolumu V1 sayfa yapisina indirildi.
- Panel tek kart olarak netlestirildi: ust ozet bari, mod secici, kaynak secimi, tek primary aksiyon, sonuc ozeti, fark alani ve kisa gecmis.
- V1 disinda kalacaklar da acik yazildi: tam scrape console, ham JSON/HTML, task-note duvari ve schedule mantigi yok.

## Hazirlik / Tarama panel UI iskeleti
- Business Detail sayfasina yeni `BusinessScanPanel` component'i baglandi.
- Audit snapshot kartindaki manuel yenileme aksiyonu panelin icine tasindi; audit karti ozet alani olarak sade kaldi.
- Panelde mod secici, secmeli kaynak listesi, tek primary `Tarama baslat` aksiyonu, sonuc ozeti, fark alani ve kisa gecmis gorunur hale getirildi.
- `BusinessDiscoveryRefreshButton` esnek label/helper text alacak sekilde genisletildi ve `npm run build` tekrar gecti.

## Tarama secimlerinin run metadata'ya baglanmasi
- `refresh-discovery` route'u artik `mode` ve `sources` body alanlarini kabul ediyor.
- Run metadata'sinda `refreshMode` ve `selectedSources` saklaniyor; panel ust bandi ve kisa gecmis son secimi gercek snapshot'tan okuyor.
- Deep mode icin ayni endpoint uzerinden izole runtime smoke test alindi; response ve sayfa HTML'inde `Son mod: Derin` ve `Son kaynak sayisi: 2` goruldu.
- Ana runtime deploy'u henuz alinmadi; bu degisiklik workspace tarafinda hazir.

## Ana runtime deploy ve smoke sonucu
- `agent-workspace` uzerinde production build alindi, `esnafdijital-admin.service` restart edildi ve `3012` uzerindeki canli admin runtime yeni tarama paneli secimlerini almaya basladi.
- Canli business detail sayfasinda `Tarama paneli V1`, `Son mod` ve `Son kaynak sayisi` rozetleri gorunur dogrulandi.
- `cmoaahlki0002o5ytif0fyazu` kaydina `mode=deep` ve iki kaynak (`maps-refresh`, `instagram`) ile POST `/api/businesses/:id/refresh-discovery` `200` dondu; response metadata'sinda `refreshMode=deep` ve `selectedSources` iki eleman olarak yazildi.
- Ayni smoke testte daha kritik bir kalite sorunu da goruldu: `Arnavutkoy Pilot Berber` aramasi `Pilot Garage Oto Ekspertiz Arnavutkoy` kaydina yanlis eslesti. Sonraki teknik adim manual refresh match guard'ini sertlestirmek.

## Businesses index V1
- `agent-workspace/app/businesses/page.tsx` ile ayri `Businesses` liste sayfasi acildi.
- Sayfa `Project OS` kuyrugundan ayri olarak tum isletmeleri arama, segment, isletme durumu ve hat asamasi filtreleriyle listeliyor.
- Satirdan hem business detail route'una hem de `Project OS` icindeki ilgili kayda gecis linkleri eklendi.
- Sol menuye `Isletmeler` baglantisi eklendi, alt route'larda aktif menu vurgusu `startsWith` cizgisine alindi ve `Project OS` hero alanina yeni sayfaya gecis linki kondu.
- `npm run build` tekrar gecti.

## Business detail sadeleştirme
- Business detail sayfasinda tekrar eden `Operasyon özeti` blogu kaldirildi.
- Ust ozet rozetleri de sadeleştirildi; yalnizca kayit durumu ve aktif asama tutuldu.
- Boylece `Next step`, `Audit snapshot` ve alt kayitlar arasinda ayni bilginin ust uste tekrar etmesi azaltildi.
- Build alindi, servis restart edildi ve canli sayfada sade hali gorunur dogrulandi.
- Devam sadeleştirmede business detail icindeki ust sayac kartlari kaldirildi.
- `Audit snapshot` blogu `Dis veri ozeti` olarak daraltildi; ayni audit summary/status metnini tekrar etmek yerine sadece dis sinyali gostermeye cekildi.
- `Temel profil` kartindaki teknik `kanonik route` satiri da kaldirildi.
- Son turda `Temel profil` karti tamamen kaldirildi; ust baslik ve badge alani ayni bilgiyi yeterince tasidigi icin detay sayfasi daha temiz hale geldi.
- `Isletme detay` ve `Next step` ayri kartlari da tek ust blokta birlestirildi.
- Ust blok artik ad, kisa kimlik satiri, durum/asama rozetleri, tek `Siradaki adim` satiri ve ana aksiyonlari birlikte tasiyor.

## Business detail sifirdan ekran reseti
- Kurucu business detail ekranini sifirdan ele aldi ve bu yuzeyi sadece uc cekirdege indirdi: `Isletme karti`, `Y.Z raporu`, `Tarama paneli`.
- Ustteki `Isletme karti` temel bilgilerle yeniden kuruldu: isletme adi, adres, telefon, isletme turu, muhatap, Google Maps linki, website, varsa Instagram ve kisa ic not.
- `Y.Z raporu` alani bos state + `Rapor olustur` butonlu placeholder olarak eklendi; skill tetikleme mantigi sonra baglanacak.
- `Tarama paneli` iki ana secime indirildi: `Ajan tarama` ve `Apify tarama`.
- `Ajan tarama` tarafi su an sadece tasarim placeholder'i; sonradan skill baglanacak.
- `Apify tarama` tarafinda secilebilir kaynaklar `Google Maps`, `Yandex`, `Apple Maps`, `Google Search` olarak yeniden yazildi.
- `refresh-discovery` route'u yeni `apify` mode ve bu kaynak adlarini kabul edecek sekilde guncellendi.
- Build alindi, servis restart edildi ve canli sayfada yeni iskelet gorunur dogrulandi.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.

## Plugin ve scrape arastirmasi
- Scrape tarafini guclendirebilecek bundled adaylar `firecrawl`, `exa`, `browser`, `duckduckgo`, `brave` olarak kontrol edildi; bu ortamda allowlist disinda kaldiklari icin aktif kullanima gecmedi.
- Kisa maliyet sonucu: `Firecrawl` tek seferlik sinirli free kredi ile basliyor, `Exa` esasen ucretli, `Brave` API key gerektiriyor; bu yuzden yakin vadede ucretsiz/guvenilir omurga `internal website check + mevcut web arastirmasi + Apify` cizgisinde kaldi.

## Agent browser kurulumu
- Supheli gorunen browser skill adaylari elendi; temiz aday olarak `agent-browser-core` secildi ve workspace'e kuruldu. Commit: `52d1941` (`Install agent-browser-core skill`).
- Alt runtime da kuruldu: `agent-browser@0.26.0` global npm ile yuklendi, `agent-browser install` ile Chrome binary indirildi, Linux bagimlilik eksigi nedeniyle `agent-browser install --with-deps` calistirildi.
- Smoke test gecti: `agent-browser open https://example.com --session smoke-test` ve `agent-browser get title --session smoke-test` calisti, sonra session kapatildi.
- Host tarafinda ek not: kurulum sonunda kernel upgrade uyarisi geldi (`6.8.0-110-generic` bekleniyor); browser runtime icin engel degil ama host reboot planlanabilir.

## Business detail referans birlestirme
- `REFERENCES/businesses-page-capability-breakdown.md`, `REFERENCES/business-detail-data-contract-v1.md` ve `REFERENCES/businesses-page-plan.md` tek kanonik dokumanda birlestirildi: `REFERENCES/business-detail-v1.md`.
- Yeni dokuman business detail sayfasinin rolunu, veri katmanlarini, UX akisini, scrape paneli cizgisini ve V1/V2 acilis sirasini tek yerde topluyor.
- Eski uc dosya silinmedi; kirik referans olusmamasi icin `deprecated` yonlendirme notuna indirildi.

## Refresh timeline baglama
- Business detail activity timeline'a dis veri yenileme eventleri eklendi; sayfa artik business'e bagli refresh snapshot gecmisini okuyup `Dis veri snapshot yenilendi` girdileri uretiyor.
- Timeline metni arama kapsami, yorum/puan sinyali ve website var/yok ozeti tasiyor; boylece manuel `Detayli isletme verilerini al` aksiyonunun sonucu ayni kayitta gorunur oldu.
- `HEARTBEAT.md` siradaki somut adimi buna gore guncellendi: kalan is gercek kayitta canli smoke test yapmak.

## Canli smoke test notu
- `Berber Serkan` kaydinda guncel build ayri portta kaldirilarak smoke test ilerletildi; snapshot karti `23 Nis 02:08` verisini ve timeline `Dis veri snapshot yenilendi` eventini gercek kayitta gosterdi.
- Manuel yenileme API route'u ise Next runtime icinde halen `500` donuyor; disaridan ayni Apify komutu kosabildigi halde route icindeki cagri sessiz sekilde fail oluyor.
- Route catch blogu stdout/stderr detayi donecek sekilde sertlestirildi ama hata halen bos geliyor; siradaki teknik is runtime ile shell arasindaki bu farki daraltmak.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.

## Plugin ve scrape arastirmasi
- Scrape tarafini guclendirebilecek bundled adaylar `firecrawl`, `exa`, `browser`, `duckduckgo`, `brave` olarak kontrol edildi; bu ortamda allowlist disinda kaldiklari icin aktif kullanima gecmedi.
- Kisa maliyet sonucu: `Firecrawl` tek seferlik sinirli free kredi ile basliyor, `Exa` esasen ucretli, `Brave` API key gerektiriyor; bu yuzden yakin vadede ucretsiz/guvenilir omurga `internal website check + mevcut web arastirmasi + Apify` cizgisinde kaldi.

## Agent browser kurulumu
- Supheli gorunen browser skill adaylari elendi; temiz aday olarak `agent-browser-core` secildi ve workspace'e kuruldu. Commit: `52d1941` (`Install agent-browser-core skill`).
- Alt runtime da kuruldu: `agent-browser@0.26.0` global npm ile yuklendi, `agent-browser install` ile Chrome binary indirildi, Linux bagimlilik eksigi nedeniyle `agent-browser install --with-deps` calistirildi.
- Smoke test gecti: `agent-browser open https://example.com --session smoke-test` ve `agent-browser get title --session smoke-test` calisti, sonra session kapatildi.
- Host tarafinda ek not: kurulum sonunda kernel upgrade uyarisi geldi (`6.8.0-110-generic` bekleniyor); browser runtime icin engel degil ama host reboot planlanabilir.

## Business detail referans birlestirme
- `REFERENCES/businesses-page-capability-breakdown.md`, `REFERENCES/business-detail-data-contract-v1.md` ve `REFERENCES/businesses-page-plan.md` tek kanonik dokumanda birlestirildi: `REFERENCES/business-detail-v1.md`.
- Yeni dokuman business detail sayfasinin rolunu, veri katmanlarini, UX akisini, scrape paneli cizgisini ve V1/V2 acilis sirasini tek yerde topluyor.
- Eski uc dosya silinmedi; kirik referans olusmamasi icin `deprecated` yonlendirme notuna indirildi.

## Refresh timeline baglama
- Business detail activity timeline'a dis veri yenileme eventleri eklendi; sayfa artik business'e bagli refresh snapshot gecmisini okuyup `Dis veri snapshot yenilendi` girdileri uretiyor.
- Timeline metni arama kapsami, yorum/puan sinyali ve website var/yok ozeti tasiyor; boylece manuel `Detayli isletme verilerini al` aksiyonunun sonucu ayni kayitta gorunur oldu.
- `HEARTBEAT.md` siradaki somut adimi buna gore guncellendi: kalan is gercek kayitta canli smoke test yapmak.

## Canli smoke test notu
- `Berber Serkan` kaydinda guncel build ayri portta kaldirilarak smoke test ilerletildi; snapshot karti `23 Nis 02:08` verisini ve timeline `Dis veri snapshot yenilendi` eventini gercek kayitta gosterdi.
- Manuel yenileme API route'u ise Next runtime icinde halen `500` donuyor; disaridan ayni Apify komutu kosabildigi halde route icindeki cagri sessiz sekilde fail oluyor.
- Route catch blogu stdout/stderr detayi donecek sekilde sertlestirildi ama hata halen bos geliyor; siradaki teknik is runtime ile shell arasindaki bu farki daraltmak.

## Deep research oncelik listesi
- En degerli arastirma basliklari su sirada netlesti: `teklif omurgasi V1`, `Business Detail vs Project OS siniri`, `hafif tarama / derin tarama urun kontrati`, `audit snapshot veri modeli`, `operasyonel CRM ama yatay SaaS degil`, `Hesap Merkezi V2 bilgi mimarisi`, `delivery scope standardi`.
- Ikinci halka arastirma basliklari: `ilk 3 segment icin teklif-persona farklari`, `kanonik isletme kaydi yasam dongusu`, `Consultation Center karar hatti`.

## Refresh route kapanis notu
- `refresh-discovery` 500'unun asil nedeni Apify komutunun kendisi degil, route icindeki `execFile` bekleme hattiydi; saf shell ayni komutu `41s` civarinda tamamlarken `execFile` child pipe'larini acik tuttugu icin timeout'a gidiyordu.
- Route `spawn + stdio ignore` cizgisine alindi ve timeout `300s` olarak korundu; build tekrar gecti.
- Izole runtime smoke testinde `Berber Serkan` kaydina POST `/api/businesses/:id/refresh-discovery` `200` dondu, yeni snapshot yazildi ve Business Detail sayfasinda `23 Nis 03:00` snapshot zamani ile timeline girdisi gorundu.
- Ardindan ana runtime deploy edildi, `3012` uzerinde ayni endpoint tekrar `200` verdi ve blocker kapandi.

## Hazirlik / Tarama panel dokumani
- `REFERENCES/business-detail-v1.md` icindeki `Hazirlik / Tarama paneli` bolumu V1 sayfa yapisina indirildi.
- Panel tek kart olarak netlestirildi: ust ozet bari, mod secici, kaynak secimi, tek primary aksiyon, sonuc ozeti, fark alani ve kisa gecmis.
- V1 disinda kalacaklar da acik yazildi: tam scrape console, ham JSON/HTML, task-note duvari ve schedule mantigi yok.

## Hazirlik / Tarama panel UI iskeleti
- Business Detail sayfasina yeni `BusinessScanPanel` component'i baglandi.
- Audit snapshot kartindaki manuel yenileme aksiyonu panelin icine tasindi; audit karti ozet alani olarak sade kaldi.
- Panelde mod secici, secmeli kaynak listesi, tek primary `Tarama baslat` aksiyonu, sonuc ozeti, fark alani ve kisa gecmis gorunur hale getirildi.
- `BusinessDiscoveryRefreshButton` esnek label/helper text alacak sekilde genisletildi ve `npm run build` tekrar gecti.

## Tarama secimlerinin run metadata'ya baglanmasi
- `refresh-discovery` route'u artik `mode` ve `sources` body alanlarini kabul ediyor.
- Run metadata'sinda `refreshMode` ve `selectedSources` saklaniyor; panel ust bandi ve kisa gecmis son secimi gercek snapshot'tan okuyor.
- Deep mode icin ayni endpoint uzerinden izole runtime smoke test alindi; response ve sayfa HTML'inde `Son mod: Derin` ve `Son kaynak sayisi: 2` goruldu.
- Ana runtime deploy'u henuz alinmadi; bu degisiklik workspace tarafinda hazir.

## Ana runtime deploy ve smoke sonucu
- `agent-workspace` uzerinde production build alindi, `esnafdijital-admin.service` restart edildi ve `3012` uzerindeki canli admin runtime yeni tarama paneli secimlerini almaya basladi.
- Canli business detail sayfasinda `Tarama paneli V1`, `Son mod` ve `Son kaynak sayisi` rozetleri gorunur dogrulandi.
- `cmoaahlki0002o5ytif0fyazu` kaydina `mode=deep` ve iki kaynak (`maps-refresh`, `instagram`) ile POST `/api/businesses/:id/refresh-discovery` `200` dondu; response metadata'sinda `refreshMode=deep` ve `selectedSources` iki eleman olarak yazildi.
- Ayni smoke testte daha kritik bir kalite sorunu da goruldu: `Arnavutkoy Pilot Berber` aramasi `Pilot Garage Oto Ekspertiz Arnavutkoy` kaydina yanlis eslesti. Sonraki teknik adim manual refresh match guard'ini sertlestirmek.

## Businesses index V1
- `agent-workspace/app/businesses/page.tsx` ile ayri `Businesses` liste sayfasi acildi.
- Sayfa `Project OS` kuyrugundan ayri olarak tum isletmeleri arama, segment, isletme durumu ve hat asamasi filtreleriyle listeliyor.
- Satirdan hem business detail route'una hem de `Project OS` icindeki ilgili kayda gecis linkleri eklendi.
- Sol menuye `Isletmeler` baglantisi eklendi, alt route'larda aktif menu vurgusu `startsWith` cizgisine alindi ve `Project OS` hero alanina yeni sayfaya gecis linki kondu.
- `npm run build` tekrar gecti.

## Business detail sadeleştirme
- Business detail sayfasinda tekrar eden `Operasyon özeti` blogu kaldirildi.
- Ust ozet rozetleri de sadeleştirildi; yalnizca kayit durumu ve aktif asama tutuldu.
- Boylece `Next step`, `Audit snapshot` ve alt kayitlar arasinda ayni bilginin ust uste tekrar etmesi azaltildi.
- Build alindi, servis restart edildi ve canli sayfada sade hali gorunur dogrulandi.
- Devam sadeleştirmede business detail icindeki ust sayac kartlari kaldirildi.
- `Audit snapshot` blogu `Dis veri ozeti` olarak daraltildi; ayni audit summary/status metnini tekrar etmek yerine sadece dis sinyali gostermeye cekildi.
- `Temel profil` kartindaki teknik `kanonik route` satiri da kaldirildi.
- Son turda `Temel profil` karti tamamen kaldirildi; ust baslik ve badge alani ayni bilgiyi yeterince tasidigi icin detay sayfasi daha temiz hale geldi.
- `Isletme detay` ve `Next step` ayri kartlari da tek ust blokta birlestirildi.
- Ust blok artik ad, kisa kimlik satiri, durum/asama rozetleri, tek `Siradaki adim` satiri ve ana aksiyonlari birlikte tasiyor.

## Business detail sifirdan ekran reseti
- Kurucu business detail ekranini sifirdan ele aldi ve bu yuzeyi sadece uc cekirdege indirdi: `Isletme karti`, `Y.Z raporu`, `Tarama paneli`.
- Ustteki `Isletme karti` temel bilgilerle yeniden kuruldu: isletme adi, adres, telefon, isletme turu, muhatap, Google Maps linki, website, varsa Instagram ve kisa ic not.
- `Y.Z raporu` alani bos state + `Rapor olustur` butonlu placeholder olarak eklendi; skill tetikleme mantigi sonra baglanacak.
- `Tarama paneli` iki ana secime indirildi: `Ajan tarama` ve `Apify tarama`.
- `Ajan tarama` tarafi su an sadece tasarim placeholder'i; sonradan skill baglanacak.
- `Apify tarama` tarafinda secilebilir kaynaklar `Google Maps`, `Yandex`, `Apple Maps`, `Google Search` olarak yeniden yazildi.
- `refresh-discovery` route'u yeni `apify` mode ve bu kaynak adlarini kabul edecek sekilde guncellendi.
- Build alindi, servis restart edildi ve canli sayfada yeni iskelet gorunur dogrulandi.

## Kurucu ile ekran planlama calisma tarzi
- Business detail ekranini yeniden kurarken kurucu acik tercih verdi: fikir istemedikce ajan yorum veya alternatif oneriler sunmayacak; kullanicinin madde madde verdigi istekleri netlestirip duzenleyecek.
- Bu planlama modunda once gereksinim toplama, sonra uygulama akisi izlenecek; ekstra urun yonu ancak kullanici sorarsa verilecek.

## Business detail UI yogunluk ayari
- Kurucu business detail kartlarinda gereksiz bosluk istemiyor; alanlar daha kompakt ve yatay okunur olmali.
- Apify kaynak secimleri buyuk ve dikey degil, kucuk ve yatay wrap eden secimler olarak tutulacak.

## Business detail spacing tanisi ve alan duzeni
- Kurucu, business detail kartinda ozellikle etiket (`Adres`) ile altindaki deger metni arasindaki boslugun fazla oldugunu netlestirdi; genel kart spacing degil, alan-ici label/value mesafesi odak oldu.
- Sorunun kaynagi uc spacing katmaninin ust uste binmesiydi: `.eyebrow` alt margin, `stack-xs` gap ve normal `p` varsayilan margin.
- Bu yuzden business detail alanlari icin ayri ve daha kompakt `detail-field` duzeni tanimlandi; label ve deger margin'leri sifirlanip aradaki bosluk tek kaynaktan yonetilecek sekilde `4px` cizgisine cekildi.
- Canli duzeltme build + restart ile deploy edildi. Commit: `c6b0fe3` (`Tighten business detail field label spacing`).
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.

## Plugin ve scrape arastirmasi
- Scrape tarafini guclendirebilecek bundled adaylar `firecrawl`, `exa`, `browser`, `duckduckgo`, `brave` olarak kontrol edildi; bu ortamda allowlist disinda kaldiklari icin aktif kullanima gecmedi.
- Kisa maliyet sonucu: `Firecrawl` tek seferlik sinirli free kredi ile basliyor, `Exa` esasen ucretli, `Brave` API key gerektiriyor; bu yuzden yakin vadede ucretsiz/guvenilir omurga `internal website check + mevcut web arastirmasi + Apify` cizgisinde kaldi.

## Agent browser kurulumu
- Supheli gorunen browser skill adaylari elendi; temiz aday olarak `agent-browser-core` secildi ve workspace'e kuruldu. Commit: `52d1941` (`Install agent-browser-core skill`).
- Alt runtime da kuruldu: `agent-browser@0.26.0` global npm ile yuklendi, `agent-browser install` ile Chrome binary indirildi, Linux bagimlilik eksigi nedeniyle `agent-browser install --with-deps` calistirildi.
- Smoke test gecti: `agent-browser open https://example.com --session smoke-test` ve `agent-browser get title --session smoke-test` calisti, sonra session kapatildi.
- Host tarafinda ek not: kurulum sonunda kernel upgrade uyarisi geldi (`6.8.0-110-generic` bekleniyor); browser runtime icin engel degil ama host reboot planlanabilir.

## Business detail referans birlestirme
- `REFERENCES/businesses-page-capability-breakdown.md`, `REFERENCES/business-detail-data-contract-v1.md` ve `REFERENCES/businesses-page-plan.md` tek kanonik dokumanda birlestirildi: `REFERENCES/business-detail-v1.md`.
- Yeni dokuman business detail sayfasinin rolunu, veri katmanlarini, UX akisini, scrape paneli cizgisini ve V1/V2 acilis sirasini tek yerde topluyor.
- Eski uc dosya silinmedi; kirik referans olusmamasi icin `deprecated` yonlendirme notuna indirildi.

## Refresh timeline baglama
- Business detail activity timeline'a dis veri yenileme eventleri eklendi; sayfa artik business'e bagli refresh snapshot gecmisini okuyup `Dis veri snapshot yenilendi` girdileri uretiyor.
- Timeline metni arama kapsami, yorum/puan sinyali ve website var/yok ozeti tasiyor; boylece manuel `Detayli isletme verilerini al` aksiyonunun sonucu ayni kayitta gorunur oldu.
- `HEARTBEAT.md` siradaki somut adimi buna gore guncellendi: kalan is gercek kayitta canli smoke test yapmak.

## Canli smoke test notu
- `Berber Serkan` kaydinda guncel build ayri portta kaldirilarak smoke test ilerletildi; snapshot karti `23 Nis 02:08` verisini ve timeline `Dis veri snapshot yenilendi` eventini gercek kayitta gosterdi.
- Manuel yenileme API route'u ise Next runtime icinde halen `500` donuyor; disaridan ayni Apify komutu kosabildigi halde route icindeki cagri sessiz sekilde fail oluyor.
- Route catch blogu stdout/stderr detayi donecek sekilde sertlestirildi ama hata halen bos geliyor; siradaki teknik is runtime ile shell arasindaki bu farki daraltmak.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.

## Plugin ve scrape arastirmasi
- Scrape tarafini guclendirebilecek bundled adaylar `firecrawl`, `exa`, `browser`, `duckduckgo`, `brave` olarak kontrol edildi; bu ortamda allowlist disinda kaldiklari icin aktif kullanima gecmedi.
- Kisa maliyet sonucu: `Firecrawl` tek seferlik sinirli free kredi ile basliyor, `Exa` esasen ucretli, `Brave` API key gerektiriyor; bu yuzden yakin vadede ucretsiz/guvenilir omurga `internal website check + mevcut web arastirmasi + Apify` cizgisinde kaldi.

## Agent browser kurulumu
- Supheli gorunen browser skill adaylari elendi; temiz aday olarak `agent-browser-core` secildi ve workspace'e kuruldu. Commit: `52d1941` (`Install agent-browser-core skill`).
- Alt runtime da kuruldu: `agent-browser@0.26.0` global npm ile yuklendi, `agent-browser install` ile Chrome binary indirildi, Linux bagimlilik eksigi nedeniyle `agent-browser install --with-deps` calistirildi.
- Smoke test gecti: `agent-browser open https://example.com --session smoke-test` ve `agent-browser get title --session smoke-test` calisti, sonra session kapatildi.
- Host tarafinda ek not: kurulum sonunda kernel upgrade uyarisi geldi (`6.8.0-110-generic` bekleniyor); browser runtime icin engel degil ama host reboot planlanabilir.

## Business detail referans birlestirme
- `REFERENCES/businesses-page-capability-breakdown.md`, `REFERENCES/business-detail-data-contract-v1.md` ve `REFERENCES/businesses-page-plan.md` tek kanonik dokumanda birlestirildi: `REFERENCES/business-detail-v1.md`.
- Yeni dokuman business detail sayfasinin rolunu, veri katmanlarini, UX akisini, scrape paneli cizgisini ve V1/V2 acilis sirasini tek yerde topluyor.
- Eski uc dosya silinmedi; kirik referans olusmamasi icin `deprecated` yonlendirme notuna indirildi.

## Refresh timeline baglama
- Business detail activity timeline'a dis veri yenileme eventleri eklendi; sayfa artik business'e bagli refresh snapshot gecmisini okuyup `Dis veri snapshot yenilendi` girdileri uretiyor.
- Timeline metni arama kapsami, yorum/puan sinyali ve website var/yok ozeti tasiyor; boylece manuel `Detayli isletme verilerini al` aksiyonunun sonucu ayni kayitta gorunur oldu.
- `HEARTBEAT.md` siradaki somut adimi buna gore guncellendi: kalan is gercek kayitta canli smoke test yapmak.

## Canli smoke test notu
- `Berber Serkan` kaydinda guncel build ayri portta kaldirilarak smoke test ilerletildi; snapshot karti `23 Nis 02:08` verisini ve timeline `Dis veri snapshot yenilendi` eventini gercek kayitta gosterdi.
- Manuel yenileme API route'u ise Next runtime icinde halen `500` donuyor; disaridan ayni Apify komutu kosabildigi halde route icindeki cagri sessiz sekilde fail oluyor.
- Route catch blogu stdout/stderr detayi donecek sekilde sertlestirildi ama hata halen bos geliyor; siradaki teknik is runtime ile shell arasindaki bu farki daraltmak.

## Deep research oncelik listesi
- En degerli arastirma basliklari su sirada netlesti: `teklif omurgasi V1`, `Business Detail vs Project OS siniri`, `hafif tarama / derin tarama urun kontrati`, `audit snapshot veri modeli`, `operasyonel CRM ama yatay SaaS degil`, `Hesap Merkezi V2 bilgi mimarisi`, `delivery scope standardi`.
- Ikinci halka arastirma basliklari: `ilk 3 segment icin teklif-persona farklari`, `kanonik isletme kaydi yasam dongusu`, `Consultation Center karar hatti`.

## Refresh route kapanis notu
- `refresh-discovery` 500'unun asil nedeni Apify komutunun kendisi degil, route icindeki `execFile` bekleme hattiydi; saf shell ayni komutu `41s` civarinda tamamlarken `execFile` child pipe'larini acik tuttugu icin timeout'a gidiyordu.
- Route `spawn + stdio ignore` cizgisine alindi ve timeout `300s` olarak korundu; build tekrar gecti.
- Izole runtime smoke testinde `Berber Serkan` kaydina POST `/api/businesses/:id/refresh-discovery` `200` dondu, yeni snapshot yazildi ve Business Detail sayfasinda `23 Nis 03:00` snapshot zamani ile timeline girdisi gorundu.
- Ardindan ana runtime deploy edildi, `3012` uzerinde ayni endpoint tekrar `200` verdi ve blocker kapandi.

## Hazirlik / Tarama panel dokumani
- `REFERENCES/business-detail-v1.md` icindeki `Hazirlik / Tarama paneli` bolumu V1 sayfa yapisina indirildi.
- Panel tek kart olarak netlestirildi: ust ozet bari, mod secici, kaynak secimi, tek primary aksiyon, sonuc ozeti, fark alani ve kisa gecmis.
- V1 disinda kalacaklar da acik yazildi: tam scrape console, ham JSON/HTML, task-note duvari ve schedule mantigi yok.

## Hazirlik / Tarama panel UI iskeleti
- Business Detail sayfasina yeni `BusinessScanPanel` component'i baglandi.
- Audit snapshot kartindaki manuel yenileme aksiyonu panelin icine tasindi; audit karti ozet alani olarak sade kaldi.
- Panelde mod secici, secmeli kaynak listesi, tek primary `Tarama baslat` aksiyonu, sonuc ozeti, fark alani ve kisa gecmis gorunur hale getirildi.
- `BusinessDiscoveryRefreshButton` esnek label/helper text alacak sekilde genisletildi ve `npm run build` tekrar gecti.

## Tarama secimlerinin run metadata'ya baglanmasi
- `refresh-discovery` route'u artik `mode` ve `sources` body alanlarini kabul ediyor.
- Run metadata'sinda `refreshMode` ve `selectedSources` saklaniyor; panel ust bandi ve kisa gecmis son secimi gercek snapshot'tan okuyor.
- Deep mode icin ayni endpoint uzerinden izole runtime smoke test alindi; response ve sayfa HTML'inde `Son mod: Derin` ve `Son kaynak sayisi: 2` goruldu.
- Ana runtime deploy'u henuz alinmadi; bu degisiklik workspace tarafinda hazir.

## Ana runtime deploy ve smoke sonucu
- `agent-workspace` uzerinde production build alindi, `esnafdijital-admin.service` restart edildi ve `3012` uzerindeki canli admin runtime yeni tarama paneli secimlerini almaya basladi.
- Canli business detail sayfasinda `Tarama paneli V1`, `Son mod` ve `Son kaynak sayisi` rozetleri gorunur dogrulandi.
- `cmoaahlki0002o5ytif0fyazu` kaydina `mode=deep` ve iki kaynak (`maps-refresh`, `instagram`) ile POST `/api/businesses/:id/refresh-discovery` `200` dondu; response metadata'sinda `refreshMode=deep` ve `selectedSources` iki eleman olarak yazildi.
- Ayni smoke testte daha kritik bir kalite sorunu da goruldu: `Arnavutkoy Pilot Berber` aramasi `Pilot Garage Oto Ekspertiz Arnavutkoy` kaydina yanlis eslesti. Sonraki teknik adim manual refresh match guard'ini sertlestirmek.

## Businesses index V1
- `agent-workspace/app/businesses/page.tsx` ile ayri `Businesses` liste sayfasi acildi.
- Sayfa `Project OS` kuyrugundan ayri olarak tum isletmeleri arama, segment, isletme durumu ve hat asamasi filtreleriyle listeliyor.
- Satirdan hem business detail route'una hem de `Project OS` icindeki ilgili kayda gecis linkleri eklendi.
- Sol menuye `Isletmeler` baglantisi eklendi, alt route'larda aktif menu vurgusu `startsWith` cizgisine alindi ve `Project OS` hero alanina yeni sayfaya gecis linki kondu.
- `npm run build` tekrar gecti.

## Business detail sadeleştirme
- Business detail sayfasinda tekrar eden `Operasyon özeti` blogu kaldirildi.
- Ust ozet rozetleri de sadeleştirildi; yalnizca kayit durumu ve aktif asama tutuldu.
- Boylece `Next step`, `Audit snapshot` ve alt kayitlar arasinda ayni bilginin ust uste tekrar etmesi azaltildi.
- Build alindi, servis restart edildi ve canli sayfada sade hali gorunur dogrulandi.
- Devam sadeleştirmede business detail icindeki ust sayac kartlari kaldirildi.
- `Audit snapshot` blogu `Dis veri ozeti` olarak daraltildi; ayni audit summary/status metnini tekrar etmek yerine sadece dis sinyali gostermeye cekildi.
- `Temel profil` kartindaki teknik `kanonik route` satiri da kaldirildi.
- Son turda `Temel profil` karti tamamen kaldirildi; ust baslik ve badge alani ayni bilgiyi yeterince tasidigi icin detay sayfasi daha temiz hale geldi.
- `Isletme detay` ve `Next step` ayri kartlari da tek ust blokta birlestirildi.
- Ust blok artik ad, kisa kimlik satiri, durum/asama rozetleri, tek `Siradaki adim` satiri ve ana aksiyonlari birlikte tasiyor.

## Business detail sifirdan ekran reseti
- Kurucu business detail ekranini sifirdan ele aldi ve bu yuzeyi sadece uc cekirdege indirdi: `Isletme karti`, `Y.Z raporu`, `Tarama paneli`.
- Ustteki `Isletme karti` temel bilgilerle yeniden kuruldu: isletme adi, adres, telefon, isletme turu, muhatap, Google Maps linki, website, varsa Instagram ve kisa ic not.
- `Y.Z raporu` alani bos state + `Rapor olustur` butonlu placeholder olarak eklendi; skill tetikleme mantigi sonra baglanacak.
- `Tarama paneli` iki ana secime indirildi: `Ajan tarama` ve `Apify tarama`.
- `Ajan tarama` tarafi su an sadece tasarim placeholder'i; sonradan skill baglanacak.
- `Apify tarama` tarafinda secilebilir kaynaklar `Google Maps`, `Yandex`, `Apple Maps`, `Google Search` olarak yeniden yazildi.
- `refresh-discovery` route'u yeni `apify` mode ve bu kaynak adlarini kabul edecek sekilde guncellendi.
- Build alindi, servis restart edildi ve canli sayfada yeni iskelet gorunur dogrulandi.

## Kurucu ile ekran planlama calisma tarzi
- Business detail ekranini yeniden kurarken kurucu acik tercih verdi: fikir istemedikce ajan yorum veya alternatif oneriler sunmayacak; kullanicinin madde madde verdigi istekleri netlestirip duzenleyecek.
- Bu planlama modunda once gereksinim toplama, sonra uygulama akisi izlenecek; ekstra urun yonu ancak kullanici sorarsa verilecek.

## Business detail UI yogunluk ayari
- Kurucu business detail kartlarinda gereksiz bosluk istemiyor; alanlar daha kompakt ve yatay okunur olmali.
- Apify kaynak secimleri buyuk ve dikey degil, kucuk ve yatay wrap eden secimler olarak tutulacak.

## Business detail spacing tanisi ve alan duzeni
- Kurucu, business detail kartinda ozellikle etiket (`Adres`) ile altindaki deger metni arasindaki boslugun fazla oldugunu netlestirdi; genel kart spacing degil, alan-ici label/value mesafesi odak oldu.
- Sorunun kaynagi uc spacing katmaninin ust uste binmesiydi: `.eyebrow` alt margin, `stack-xs` gap ve normal `p` varsayilan margin.
- Bu yuzden business detail alanlari icin ayri ve daha kompakt `detail-field` duzeni tanimlandi; label ve deger margin'leri sifirlanip aradaki bosluk tek kaynaktan yonetilecek sekilde `4px` cizgisine cekildi.
- Canli duzeltme build + restart ile deploy edildi. Commit: `c6b0fe3` (`Tighten business detail field label spacing`).

## Business detail tarama ve rapor hattinin baglanmasi
- `Ajan tarama` artik gercek hatta bagli: `/api/businesses/[id]/agent-scan` route'u business karti + discovery snapshot + audit ozetini skill'e verip sonucu `state/agent-scans/<businessId>.json` history olarak kaydediyor.
- Business detail sayfasi son ajan tarama sonucunu `Son durum`, `Kisa ozet`, `Bulgular`, `Eksikler / riskler`, `Onerilen sonraki adim` alanlariyla gosteriyor.
- Apify tarafinda `Tarama gecmisi` alani eklendi; ayni isletme icin hem `Ajan tarama` hem `Apify tarama` calisma zamanlari tek yerde listeleniyor, Apify kaydinda taranan alanlar da gorunuyor.
- Eski Apify kayitlarinda alan metadata'si yoksa fallback olarak `Varsayilan tek isletme taramasi` gosteriliyor; yeni kayitlarda Google Maps detay ve yorum secimleri de alan listesine dusuyor.

## Y.Z raporu skill ve entegrasyonu
- `skills/esnafdijital-yz-report` skill'i operator odakli kisa isletme raporu icin eklendi; cikti kontrati `Genel durum`, `Guclu sinyaller`, `Zayif sinyaller`, `Dijital gorunum ozeti`, `Oncelikli aksiyon` basliklarina sabitlendi. Skill scaffold commit'i: `c2365a2`.
- `Rapor olustur` butonu artik gercek hatta bagli: `/api/businesses/[id]/yz-report` route'u isletme karti, son ajan tarama, son Apify tarama ve audit/ic not ozetini birlestirip raporu `state/yz-reports/<businessId>.json` history olarak kaydediyor.
- Business detail sayfasi son Y.Z raporunu gosterecek sekilde acildi; canli smoke testte `Berber Serkan` kaydinda ilk rapor uretildi ve `supheli eslesme` cizgisiyle gorundu.

## Apify calistirma izni tercihi
- Kurucu acik kural koydu: Apify tarafinda kullanici haberi olmadan ve kullanici butona basmadan hicbir arama calistirilmayacak.
- Varsayilan davranis daima manuel tetikleme olacak; ajan arka planda veya proaktif sekilde Apify kosusu baslatmayacak.

## Instagram tarama cizgisi
- Kurucu yeni net kural verdi: Instagram scrape, isletmenin Instagram hesabini bulmak icin kullanilmayacak.
- Instagram kaynagi yalnizca bilinen profil linki varsa o profilden veri cekme amaciyla kullanilacak; aksi halde profil arama yapilmayacak.
- Bu cizgi yanlis eslesme riskini azaltmak icin business detail derin tarama akisine islendi.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.

## Plugin ve scrape arastirmasi
- Scrape tarafini guclendirebilecek bundled adaylar `firecrawl`, `exa`, `browser`, `duckduckgo`, `brave` olarak kontrol edildi; bu ortamda allowlist disinda kaldiklari icin aktif kullanima gecmedi.
- Kisa maliyet sonucu: `Firecrawl` tek seferlik sinirli free kredi ile basliyor, `Exa` esasen ucretli, `Brave` API key gerektiriyor; bu yuzden yakin vadede ucretsiz/guvenilir omurga `internal website check + mevcut web arastirmasi + Apify` cizgisinde kaldi.

## Agent browser kurulumu
- Supheli gorunen browser skill adaylari elendi; temiz aday olarak `agent-browser-core` secildi ve workspace'e kuruldu. Commit: `52d1941` (`Install agent-browser-core skill`).
- Alt runtime da kuruldu: `agent-browser@0.26.0` global npm ile yuklendi, `agent-browser install` ile Chrome binary indirildi, Linux bagimlilik eksigi nedeniyle `agent-browser install --with-deps` calistirildi.
- Smoke test gecti: `agent-browser open https://example.com --session smoke-test` ve `agent-browser get title --session smoke-test` calisti, sonra session kapatildi.
- Host tarafinda ek not: kurulum sonunda kernel upgrade uyarisi geldi (`6.8.0-110-generic` bekleniyor); browser runtime icin engel degil ama host reboot planlanabilir.

## Business detail referans birlestirme
- `REFERENCES/businesses-page-capability-breakdown.md`, `REFERENCES/business-detail-data-contract-v1.md` ve `REFERENCES/businesses-page-plan.md` tek kanonik dokumanda birlestirildi: `REFERENCES/business-detail-v1.md`.
- Yeni dokuman business detail sayfasinin rolunu, veri katmanlarini, UX akisini, scrape paneli cizgisini ve V1/V2 acilis sirasini tek yerde topluyor.
- Eski uc dosya silinmedi; kirik referans olusmamasi icin `deprecated` yonlendirme notuna indirildi.

## Refresh timeline baglama
- Business detail activity timeline'a dis veri yenileme eventleri eklendi; sayfa artik business'e bagli refresh snapshot gecmisini okuyup `Dis veri snapshot yenilendi` girdileri uretiyor.
- Timeline metni arama kapsami, yorum/puan sinyali ve website var/yok ozeti tasiyor; boylece manuel `Detayli isletme verilerini al` aksiyonunun sonucu ayni kayitta gorunur oldu.
- `HEARTBEAT.md` siradaki somut adimi buna gore guncellendi: kalan is gercek kayitta canli smoke test yapmak.

## Canli smoke test notu
- `Berber Serkan` kaydinda guncel build ayri portta kaldirilarak smoke test ilerletildi; snapshot karti `23 Nis 02:08` verisini ve timeline `Dis veri snapshot yenilendi` eventini gercek kayitta gosterdi.
- Manuel yenileme API route'u ise Next runtime icinde halen `500` donuyor; disaridan ayni Apify komutu kosabildigi halde route icindeki cagri sessiz sekilde fail oluyor.
- Route catch blogu stdout/stderr detayi donecek sekilde sertlestirildi ama hata halen bos geliyor; siradaki teknik is runtime ile shell arasindaki bu farki daraltmak.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.

## Plugin ve scrape arastirmasi
- Scrape tarafini guclendirebilecek bundled adaylar `firecrawl`, `exa`, `browser`, `duckduckgo`, `brave` olarak kontrol edildi; bu ortamda allowlist disinda kaldiklari icin aktif kullanima gecmedi.
- Kisa maliyet sonucu: `Firecrawl` tek seferlik sinirli free kredi ile basliyor, `Exa` esasen ucretli, `Brave` API key gerektiriyor; bu yuzden yakin vadede ucretsiz/guvenilir omurga `internal website check + mevcut web arastirmasi + Apify` cizgisinde kaldi.

## Agent browser kurulumu
- Supheli gorunen browser skill adaylari elendi; temiz aday olarak `agent-browser-core` secildi ve workspace'e kuruldu. Commit: `52d1941` (`Install agent-browser-core skill`).
- Alt runtime da kuruldu: `agent-browser@0.26.0` global npm ile yuklendi, `agent-browser install` ile Chrome binary indirildi, Linux bagimlilik eksigi nedeniyle `agent-browser install --with-deps` calistirildi.
- Smoke test gecti: `agent-browser open https://example.com --session smoke-test` ve `agent-browser get title --session smoke-test` calisti, sonra session kapatildi.
- Host tarafinda ek not: kurulum sonunda kernel upgrade uyarisi geldi (`6.8.0-110-generic` bekleniyor); browser runtime icin engel degil ama host reboot planlanabilir.

## Business detail referans birlestirme
- `REFERENCES/businesses-page-capability-breakdown.md`, `REFERENCES/business-detail-data-contract-v1.md` ve `REFERENCES/businesses-page-plan.md` tek kanonik dokumanda birlestirildi: `REFERENCES/business-detail-v1.md`.
- Yeni dokuman business detail sayfasinin rolunu, veri katmanlarini, UX akisini, scrape paneli cizgisini ve V1/V2 acilis sirasini tek yerde topluyor.
- Eski uc dosya silinmedi; kirik referans olusmamasi icin `deprecated` yonlendirme notuna indirildi.

## Refresh timeline baglama
- Business detail activity timeline'a dis veri yenileme eventleri eklendi; sayfa artik business'e bagli refresh snapshot gecmisini okuyup `Dis veri snapshot yenilendi` girdileri uretiyor.
- Timeline metni arama kapsami, yorum/puan sinyali ve website var/yok ozeti tasiyor; boylece manuel `Detayli isletme verilerini al` aksiyonunun sonucu ayni kayitta gorunur oldu.
- `HEARTBEAT.md` siradaki somut adimi buna gore guncellendi: kalan is gercek kayitta canli smoke test yapmak.

## Canli smoke test notu
- `Berber Serkan` kaydinda guncel build ayri portta kaldirilarak smoke test ilerletildi; snapshot karti `23 Nis 02:08` verisini ve timeline `Dis veri snapshot yenilendi` eventini gercek kayitta gosterdi.
- Manuel yenileme API route'u ise Next runtime icinde halen `500` donuyor; disaridan ayni Apify komutu kosabildigi halde route icindeki cagri sessiz sekilde fail oluyor.
- Route catch blogu stdout/stderr detayi donecek sekilde sertlestirildi ama hata halen bos geliyor; siradaki teknik is runtime ile shell arasindaki bu farki daraltmak.

## Deep research oncelik listesi
- En degerli arastirma basliklari su sirada netlesti: `teklif omurgasi V1`, `Business Detail vs Project OS siniri`, `hafif tarama / derin tarama urun kontrati`, `audit snapshot veri modeli`, `operasyonel CRM ama yatay SaaS degil`, `Hesap Merkezi V2 bilgi mimarisi`, `delivery scope standardi`.
- Ikinci halka arastirma basliklari: `ilk 3 segment icin teklif-persona farklari`, `kanonik isletme kaydi yasam dongusu`, `Consultation Center karar hatti`.

## Refresh route kapanis notu
- `refresh-discovery` 500'unun asil nedeni Apify komutunun kendisi degil, route icindeki `execFile` bekleme hattiydi; saf shell ayni komutu `41s` civarinda tamamlarken `execFile` child pipe'larini acik tuttugu icin timeout'a gidiyordu.
- Route `spawn + stdio ignore` cizgisine alindi ve timeout `300s` olarak korundu; build tekrar gecti.
- Izole runtime smoke testinde `Berber Serkan` kaydina POST `/api/businesses/:id/refresh-discovery` `200` dondu, yeni snapshot yazildi ve Business Detail sayfasinda `23 Nis 03:00` snapshot zamani ile timeline girdisi gorundu.
- Ardindan ana runtime deploy edildi, `3012` uzerinde ayni endpoint tekrar `200` verdi ve blocker kapandi.

## Hazirlik / Tarama panel dokumani
- `REFERENCES/business-detail-v1.md` icindeki `Hazirlik / Tarama paneli` bolumu V1 sayfa yapisina indirildi.
- Panel tek kart olarak netlestirildi: ust ozet bari, mod secici, kaynak secimi, tek primary aksiyon, sonuc ozeti, fark alani ve kisa gecmis.
- V1 disinda kalacaklar da acik yazildi: tam scrape console, ham JSON/HTML, task-note duvari ve schedule mantigi yok.

## Hazirlik / Tarama panel UI iskeleti
- Business Detail sayfasina yeni `BusinessScanPanel` component'i baglandi.
- Audit snapshot kartindaki manuel yenileme aksiyonu panelin icine tasindi; audit karti ozet alani olarak sade kaldi.
- Panelde mod secici, secmeli kaynak listesi, tek primary `Tarama baslat` aksiyonu, sonuc ozeti, fark alani ve kisa gecmis gorunur hale getirildi.
- `BusinessDiscoveryRefreshButton` esnek label/helper text alacak sekilde genisletildi ve `npm run build` tekrar gecti.

## Tarama secimlerinin run metadata'ya baglanmasi
- `refresh-discovery` route'u artik `mode` ve `sources` body alanlarini kabul ediyor.
- Run metadata'sinda `refreshMode` ve `selectedSources` saklaniyor; panel ust bandi ve kisa gecmis son secimi gercek snapshot'tan okuyor.
- Deep mode icin ayni endpoint uzerinden izole runtime smoke test alindi; response ve sayfa HTML'inde `Son mod: Derin` ve `Son kaynak sayisi: 2` goruldu.
- Ana runtime deploy'u henuz alinmadi; bu degisiklik workspace tarafinda hazir.

## Ana runtime deploy ve smoke sonucu
- `agent-workspace` uzerinde production build alindi, `esnafdijital-admin.service` restart edildi ve `3012` uzerindeki canli admin runtime yeni tarama paneli secimlerini almaya basladi.
- Canli business detail sayfasinda `Tarama paneli V1`, `Son mod` ve `Son kaynak sayisi` rozetleri gorunur dogrulandi.
- `cmoaahlki0002o5ytif0fyazu` kaydina `mode=deep` ve iki kaynak (`maps-refresh`, `instagram`) ile POST `/api/businesses/:id/refresh-discovery` `200` dondu; response metadata'sinda `refreshMode=deep` ve `selectedSources` iki eleman olarak yazildi.
- Ayni smoke testte daha kritik bir kalite sorunu da goruldu: `Arnavutkoy Pilot Berber` aramasi `Pilot Garage Oto Ekspertiz Arnavutkoy` kaydina yanlis eslesti. Sonraki teknik adim manual refresh match guard'ini sertlestirmek.

## Businesses index V1
- `agent-workspace/app/businesses/page.tsx` ile ayri `Businesses` liste sayfasi acildi.
- Sayfa `Project OS` kuyrugundan ayri olarak tum isletmeleri arama, segment, isletme durumu ve hat asamasi filtreleriyle listeliyor.
- Satirdan hem business detail route'una hem de `Project OS` icindeki ilgili kayda gecis linkleri eklendi.
- Sol menuye `Isletmeler` baglantisi eklendi, alt route'larda aktif menu vurgusu `startsWith` cizgisine alindi ve `Project OS` hero alanina yeni sayfaya gecis linki kondu.
- `npm run build` tekrar gecti.

## Business detail sadeleştirme
- Business detail sayfasinda tekrar eden `Operasyon özeti` blogu kaldirildi.
- Ust ozet rozetleri de sadeleştirildi; yalnizca kayit durumu ve aktif asama tutuldu.
- Boylece `Next step`, `Audit snapshot` ve alt kayitlar arasinda ayni bilginin ust uste tekrar etmesi azaltildi.
- Build alindi, servis restart edildi ve canli sayfada sade hali gorunur dogrulandi.
- Devam sadeleştirmede business detail icindeki ust sayac kartlari kaldirildi.
- `Audit snapshot` blogu `Dis veri ozeti` olarak daraltildi; ayni audit summary/status metnini tekrar etmek yerine sadece dis sinyali gostermeye cekildi.
- `Temel profil` kartindaki teknik `kanonik route` satiri da kaldirildi.
- Son turda `Temel profil` karti tamamen kaldirildi; ust baslik ve badge alani ayni bilgiyi yeterince tasidigi icin detay sayfasi daha temiz hale geldi.
- `Isletme detay` ve `Next step` ayri kartlari da tek ust blokta birlestirildi.
- Ust blok artik ad, kisa kimlik satiri, durum/asama rozetleri, tek `Siradaki adim` satiri ve ana aksiyonlari birlikte tasiyor.

## Business detail sifirdan ekran reseti
- Kurucu business detail ekranini sifirdan ele aldi ve bu yuzeyi sadece uc cekirdege indirdi: `Isletme karti`, `Y.Z raporu`, `Tarama paneli`.
- Ustteki `Isletme karti` temel bilgilerle yeniden kuruldu: isletme adi, adres, telefon, isletme turu, muhatap, Google Maps linki, website, varsa Instagram ve kisa ic not.
- `Y.Z raporu` alani bos state + `Rapor olustur` butonlu placeholder olarak eklendi; skill tetikleme mantigi sonra baglanacak.
- `Tarama paneli` iki ana secime indirildi: `Ajan tarama` ve `Apify tarama`.
- `Ajan tarama` tarafi su an sadece tasarim placeholder'i; sonradan skill baglanacak.
- `Apify tarama` tarafinda secilebilir kaynaklar `Google Maps`, `Yandex`, `Apple Maps`, `Google Search` olarak yeniden yazildi.
- `refresh-discovery` route'u yeni `apify` mode ve bu kaynak adlarini kabul edecek sekilde guncellendi.
- Build alindi, servis restart edildi ve canli sayfada yeni iskelet gorunur dogrulandi.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.

## Plugin ve scrape arastirmasi
- Scrape tarafini guclendirebilecek bundled adaylar `firecrawl`, `exa`, `browser`, `duckduckgo`, `brave` olarak kontrol edildi; bu ortamda allowlist disinda kaldiklari icin aktif kullanima gecmedi.
- Kisa maliyet sonucu: `Firecrawl` tek seferlik sinirli free kredi ile basliyor, `Exa` esasen ucretli, `Brave` API key gerektiriyor; bu yuzden yakin vadede ucretsiz/guvenilir omurga `internal website check + mevcut web arastirmasi + Apify` cizgisinde kaldi.

## Agent browser kurulumu
- Supheli gorunen browser skill adaylari elendi; temiz aday olarak `agent-browser-core` secildi ve workspace'e kuruldu. Commit: `52d1941` (`Install agent-browser-core skill`).
- Alt runtime da kuruldu: `agent-browser@0.26.0` global npm ile yuklendi, `agent-browser install` ile Chrome binary indirildi, Linux bagimlilik eksigi nedeniyle `agent-browser install --with-deps` calistirildi.
- Smoke test gecti: `agent-browser open https://example.com --session smoke-test` ve `agent-browser get title --session smoke-test` calisti, sonra session kapatildi.
- Host tarafinda ek not: kurulum sonunda kernel upgrade uyarisi geldi (`6.8.0-110-generic` bekleniyor); browser runtime icin engel degil ama host reboot planlanabilir.

## Business detail referans birlestirme
- `REFERENCES/businesses-page-capability-breakdown.md`, `REFERENCES/business-detail-data-contract-v1.md` ve `REFERENCES/businesses-page-plan.md` tek kanonik dokumanda birlestirildi: `REFERENCES/business-detail-v1.md`.
- Yeni dokuman business detail sayfasinin rolunu, veri katmanlarini, UX akisini, scrape paneli cizgisini ve V1/V2 acilis sirasini tek yerde topluyor.
- Eski uc dosya silinmedi; kirik referans olusmamasi icin `deprecated` yonlendirme notuna indirildi.

## Refresh timeline baglama
- Business detail activity timeline'a dis veri yenileme eventleri eklendi; sayfa artik business'e bagli refresh snapshot gecmisini okuyup `Dis veri snapshot yenilendi` girdileri uretiyor.
- Timeline metni arama kapsami, yorum/puan sinyali ve website var/yok ozeti tasiyor; boylece manuel `Detayli isletme verilerini al` aksiyonunun sonucu ayni kayitta gorunur oldu.
- `HEARTBEAT.md` siradaki somut adimi buna gore guncellendi: kalan is gercek kayitta canli smoke test yapmak.

## Canli smoke test notu
- `Berber Serkan` kaydinda guncel build ayri portta kaldirilarak smoke test ilerletildi; snapshot karti `23 Nis 02:08` verisini ve timeline `Dis veri snapshot yenilendi` eventini gercek kayitta gosterdi.
- Manuel yenileme API route'u ise Next runtime icinde halen `500` donuyor; disaridan ayni Apify komutu kosabildigi halde route icindeki cagri sessiz sekilde fail oluyor.
- Route catch blogu stdout/stderr detayi donecek sekilde sertlestirildi ama hata halen bos geliyor; siradaki teknik is runtime ile shell arasindaki bu farki daraltmak.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.
# 2026-04-23

## Gece baslangic notu
- Business detail icindeki scrape hazirlik paneli icin iki mod karari netlesti: varsayilan `Hafif tarama`, operator acarsa `Derin tarama`.
- `Hafif tarama` tek isletme icin dusuk maliyetli audit on hazirligi olacak: website kontrolu, Google Search gorunurluk aramasi, Google sonuc sayfalarindan temel bilgi toplama ve mevcut Google Maps/discovery snapshot okuma.
- `Derin tarama` ise secmeli ve maliyet uyarili genis kaynak katmani olacak: Google Maps detay yenileme, Instagram, Yandex, Apple Maps ve ek zenginlestirme.
- Bu karar `REFERENCES/businesses-page-capability-breakdown.md` icine islenerek scrape panelinin genel CRM otomasyon duvarina donmemesi cizgisi korundu.
- Business detail icindeki `Detaylı işletme verilerini al` aksiyonu gercek hatta baglandi: yeni API route tek isletme icin Apify `compass/crawler-google-places` kosusu baslatiyor, sonucu business'e bagli refresh snapshot dosyasina yaziyor ve page bu snapshot'i summary fallback'inden once okuyor.
- UI tarafinda disabled buton kaldirildi, `BusinessDiscoveryRefreshButton` eklendi ve `npm run build` tekrar gecti.

## Plugin ve scrape arastirmasi
- Scrape tarafini guclendirebilecek bundled adaylar `firecrawl`, `exa`, `browser`, `duckduckgo`, `brave` olarak kontrol edildi; bu ortamda allowlist disinda kaldiklari icin aktif kullanima gecmedi.
- Kisa maliyet sonucu: `Firecrawl` tek seferlik sinirli free kredi ile basliyor, `Exa` esasen ucretli, `Brave` API key gerektiriyor; bu yuzden yakin vadede ucretsiz/guvenilir omurga `internal website check + mevcut web arastirmasi + Apify` cizgisinde kaldi.

## Agent browser kurulumu
- Supheli gorunen browser skill adaylari elendi; temiz aday olarak `agent-browser-core` secildi ve workspace'e kuruldu. Commit: `52d1941` (`Install agent-browser-core skill`).
- Alt runtime da kuruldu: `agent-browser@0.26.0` global npm ile yuklendi, `agent-browser install` ile Chrome binary indirildi, Linux bagimlilik eksigi nedeniyle `agent-browser install --with-deps` calistirildi.
- Smoke test gecti: `agent-browser open https://example.com --session smoke-test` ve `agent-browser get title --session smoke-test` calisti, sonra session kapatildi.
- Host tarafinda ek not: kurulum sonunda kernel upgrade uyarisi geldi (`6.8.0-110-generic` bekleniyor); browser runtime icin engel degil ama host reboot planlanabilir.

## Business detail referans birlestirme
- `REFERENCES/businesses-page-capability-breakdown.md`, `REFERENCES/business-detail-data-contract-v1.md` ve `REFERENCES/businesses-page-plan.md` tek kanonik dokumanda birlestirildi: `REFERENCES/business-detail-v1.md`.
- Yeni dokuman business detail sayfasinin rolunu, veri katmanlarini, UX akisini, scrape paneli cizgisini ve V1/V2 acilis sirasini tek yerde topluyor.
- Eski uc dosya silinmedi; kirik referans olusmamasi icin `deprecated` yonlendirme notuna indirildi.

## Refresh timeline baglama
- Business detail activity timeline'a dis veri yenileme eventleri eklendi; sayfa artik business'e bagli refresh snapshot gecmisini okuyup `Dis veri snapshot yenilendi` girdileri uretiyor.
- Timeline metni arama kapsami, yorum/puan sinyali ve website var/yok ozeti tasiyor; boylece manuel `Detayli isletme verilerini al` aksiyonunun sonucu ayni kayitta gorunur oldu.
- `HEARTBEAT.md` siradaki somut adimi buna gore guncellendi: kalan is gercek kayitta canli smoke test yapmak.

## Canli smoke test notu
- `Berber Serkan` kaydinda guncel build ayri portta kaldirilarak smoke test ilerletildi; snapshot karti `23 Nis 02:08` verisini ve timeline `Dis veri snapshot yenilendi` eventini gercek kayitta gosterdi.
- Manuel yenileme API route'u ise Next runtime icinde halen `500` donuyor; disaridan ayni Apify komutu kosabildigi halde route icindeki cagri sessiz sekilde fail oluyor.
- Route catch blogu stdout/stderr detayi donecek sekilde sertlestirildi ama hata halen bos geliyor; siradaki teknik is runtime ile shell arasindaki bu farki daraltmak.

## Deep research oncelik listesi
- En degerli arastirma basliklari su sirada netlesti: `teklif omurgasi V1`, `Business Detail vs Project OS siniri`, `hafif tarama / derin tarama urun kontrati`, `audit snapshot veri modeli`, `operasyonel CRM ama yatay SaaS degil`, `Hesap Merkezi V2 bilgi mimarisi`, `delivery scope standardi`.
- Ikinci halka arastirma basliklari: `ilk 3 segment icin teklif-persona farklari`, `kanonik isletme kaydi yasam dongusu`, `Consultation Center karar hatti`.

## Refresh route kapanis notu
- `refresh-discovery` 500'unun asil nedeni Apify komutunun kendisi degil, route icindeki `execFile` bekleme hattiydi; saf shell ayni komutu `41s` civarinda tamamlarken `execFile` child pipe'larini acik tuttugu icin timeout'a gidiyordu.
- Route `spawn + stdio ignore` cizgisine alindi ve timeout `300s` olarak korundu; build tekrar gecti.
- Izole runtime smoke testinde `Berber Serkan` kaydina POST `/api/businesses/:id/refresh-discovery` `200` dondu, yeni snapshot yazildi ve Business Detail sayfasinda `23 Nis 03:00` snapshot zamani ile timeline girdisi gorundu.
- Ardindan ana runtime deploy edildi, `3012` uzerinde ayni endpoint tekrar `200` verdi ve blocker kapandi.

## Hazirlik / Tarama panel dokumani
- `REFERENCES/business-detail-v1.md` icindeki `Hazirlik / Tarama paneli` bolumu V1 sayfa yapisina indirildi.
- Panel tek kart olarak netlestirildi: ust ozet bari, mod secici, kaynak secimi, tek primary aksiyon, sonuc ozeti, fark alani ve kisa gecmis.
- V1 disinda kalacaklar da acik yazildi: tam scrape console, ham JSON/HTML, task-note duvari ve schedule mantigi yok.

## Hazirlik / Tarama panel UI iskeleti
- Business Detail sayfasina yeni `BusinessScanPanel` component'i baglandi.
- Audit snapshot kartindaki manuel yenileme aksiyonu panelin icine tasindi; audit karti ozet alani olarak sade kaldi.
- Panelde mod secici, secmeli kaynak listesi, tek primary `Tarama baslat` aksiyonu, sonuc ozeti, fark alani ve kisa gecmis gorunur hale getirildi.
- `BusinessDiscoveryRefreshButton` esnek label/helper text alacak sekilde genisletildi ve `npm run build` tekrar gecti.

## Tarama secimlerinin run metadata'ya baglanmasi
- `refresh-discovery` route'u artik `mode` ve `sources` body alanlarini kabul ediyor.
- Run metadata'sinda `refreshMode` ve `selectedSources` saklaniyor; panel ust bandi ve kisa gecmis son secimi gercek snapshot'tan okuyor.
- Deep mode icin ayni endpoint uzerinden izole runtime smoke test alindi; response ve sayfa HTML'inde `Son mod: Derin` ve `Son kaynak sayisi: 2` goruldu.
- Ana runtime deploy'u henuz alinmadi; bu degisiklik workspace tarafinda hazir.

## Ana runtime deploy ve smoke sonucu
- `agent-workspace` uzerinde production build alindi, `esnafdijital-admin.service` restart edildi ve `3012` uzerindeki canli admin runtime yeni tarama paneli secimlerini almaya basladi.
- Canli business detail sayfasinda `Tarama paneli V1`, `Son mod` ve `Son kaynak sayisi` rozetleri gorunur dogrulandi.
- `cmoaahlki0002o5ytif0fyazu` kaydina `mode=deep` ve iki kaynak (`maps-refresh`, `instagram`) ile POST `/api/businesses/:id/refresh-discovery` `200` dondu; response metadata'sinda `refreshMode=deep` ve `selectedSources` iki eleman olarak yazildi.
- Ayni smoke testte daha kritik bir kalite sorunu da goruldu: `Arnavutkoy Pilot Berber` aramasi `Pilot Garage Oto Ekspertiz Arnavutkoy` kaydina yanlis eslesti. Sonraki teknik adim manual refresh match guard'ini sertlestirmek.

## Businesses index V1
- `agent-workspace/app/businesses/page.tsx` ile ayri `Businesses` liste sayfasi acildi.
- Sayfa `Project OS` kuyrugundan ayri olarak tum isletmeleri arama, segment, isletme durumu ve hat asamasi filtreleriyle listeliyor.
- Satirdan hem business detail route'una hem de `Project OS` icindeki ilgili kayda gecis linkleri eklendi.
- Sol menuye `Isletmeler` baglantisi eklendi, alt route'larda aktif menu vurgusu `startsWith` cizgisine alindi ve `Project OS` hero alanina yeni sayfaya gecis linki kondu.
- `npm run build` tekrar gecti.

## Business detail sadeleştirme
- Business detail sayfasinda tekrar eden `Operasyon özeti` blogu kaldirildi.
- Ust ozet rozetleri de sadeleştirildi; yalnizca kayit durumu ve aktif asama tutuldu.
- Boylece `Next step`, `Audit snapshot` ve alt kayitlar arasinda ayni bilginin ust uste tekrar etmesi azaltildi.
- Build alindi, servis restart edildi ve canli sayfada sade hali gorunur dogrulandi.
- Devam sadeleştirmede business detail icindeki ust sayac kartlari kaldirildi.
- `Audit snapshot` blogu `Dis veri ozeti` olarak daraltildi; ayni audit summary/status metnini tekrar etmek yerine sadece dis sinyali gostermeye cekildi.
- `Temel profil` kartindaki teknik `kanonik route` satiri da kaldirildi.
- Son turda `Temel profil` karti tamamen kaldirildi; ust baslik ve badge alani ayni bilgiyi yeterince tasidigi icin detay sayfasi daha temiz hale geldi.
- `Isletme detay` ve `Next step` ayri kartlari da tek ust blokta birlestirildi.
- Ust blok artik ad, kisa kimlik satiri, durum/asama rozetleri, tek `Siradaki adim` satiri ve ana aksiyonlari birlikte tasiyor.

## Business detail sifirdan ekran reseti
- Kurucu business detail ekranini sifirdan ele aldi ve bu yuzeyi sadece uc cekirdege indirdi: `Isletme karti`, `Y.Z raporu`, `Tarama paneli`.
- Ustteki `Isletme karti` temel bilgilerle yeniden kuruldu: isletme adi, adres, telefon, isletme turu, muhatap, Google Maps linki, website, varsa Instagram ve kisa ic not.
- `Y.Z raporu` alani bos state + `Rapor olustur` butonlu placeholder olarak eklendi; skill tetikleme mantigi sonra baglanacak.
- `Tarama paneli` iki ana secime indirildi: `Ajan tarama` ve `Apify tarama`.
- `Ajan tarama` tarafi su an sadece tasarim placeholder'i; sonradan skill baglanacak.
- `Apify tarama` tarafinda secilebilir kaynaklar `Google Maps`, `Yandex`, `Apple Maps`, `Google Search` olarak yeniden yazildi.
- `refresh-discovery` route'u yeni `apify` mode ve bu kaynak adlarini kabul edecek sekilde guncellendi.
- Build alindi, servis restart edildi ve canli sayfada yeni iskelet gorunur dogrulandi.

## Kurucu ile ekran planlama calisma tarzi
- Business detail ekranini yeniden kurarken kurucu acik tercih verdi: fikir istemedikce ajan yorum veya alternatif oneriler sunmayacak; kullanicinin madde madde verdigi istekleri netlestirip duzenleyecek.
- Bu planlama modunda once gereksinim toplama, sonra uygulama akisi izlenecek; ekstra urun yonu ancak kullanici sorarsa verilecek.

## Business detail UI yogunluk ayari
- Kurucu business detail kartlarinda gereksiz bosluk istemiyor; alanlar daha kompakt ve yatay okunur olmali.
- Apify kaynak secimleri buyuk ve dikey degil, kucuk ve yatay wrap eden secimler olarak tutulacak.

## Business detail spacing tanisi ve alan duzeni
- Kurucu, business detail kartinda ozellikle etiket (`Adres`) ile altindaki deger metni arasindaki boslugun fazla oldugunu netlestirdi; genel kart spacing degil, alan-ici label/value mesafesi odak oldu.
- Sorunun kaynagi uc spacing katmaninin ust uste binmesiydi: `.eyebrow` alt margin, `stack-xs` gap ve normal `p` varsayilan margin.
- Bu yuzden business detail alanlari icin ayri ve daha kompakt `detail-field` duzeni tanimlandi; label ve deger margin'leri sifirlanip aradaki bosluk tek kaynaktan yonetilecek sekilde `4px` cizgisine cekildi.
- Canli duzeltme build + restart ile deploy edildi. Commit: `c6b0fe3` (`Tighten business detail field label spacing`).

## Business detail tarama ve rapor hattinin baglanmasi
- `Ajan tarama` artik gercek hatta bagli: `/api/businesses/[id]/agent-scan` route'u business karti + discovery snapshot + audit ozetini skill'e verip sonucu `state/agent-scans/<businessId>.json` history olarak kaydediyor.
- Business detail sayfasi son ajan tarama sonucunu `Son durum`, `Kisa ozet`, `Bulgular`, `Eksikler / riskler`, `Onerilen sonraki adim` alanlariyla gosteriyor.
- Apify tarafinda `Tarama gecmisi` alani eklendi; ayni isletme icin hem `Ajan tarama` hem `Apify tarama` calisma zamanlari tek yerde listeleniyor, Apify kaydinda taranan alanlar da gorunuyor.
- Eski Apify kayitlarinda alan metadata'si yoksa fallback olarak `Varsayilan tek isletme taramasi` gosteriliyor; yeni kayitlarda Google Maps detay ve yorum secimleri de alan listesine dusuyor.

## Y.Z raporu skill ve entegrasyonu
- `skills/esnafdijital-yz-report` skill'i operator odakli kisa isletme raporu icin eklendi; cikti kontrati `Genel durum`, `Guclu sinyaller`, `Zayif sinyaller`, `Dijital gorunum ozeti`, `Oncelikli aksiyon` basliklarina sabitlendi. Skill scaffold commit'i: `c2365a2`.
- `Rapor olustur` butonu artik gercek hatta bagli: `/api/businesses/[id]/yz-report` route'u isletme karti, son ajan tarama, son Apify tarama ve audit/ic not ozetini birlestirip raporu `state/yz-reports/<businessId>.json` history olarak kaydediyor.
- Business detail sayfasi son Y.Z raporunu gosterecek sekilde acildi; canli smoke testte `Berber Serkan` kaydinda ilk rapor uretildi ve `supheli eslesme` cizgisiyle gorundu.

## Apify calistirma izni tercihi
- Kurucu acik kural koydu: Apify tarafinda kullanici haberi olmadan ve kullanici butona basmadan hicbir arama calistirilmayacak.
- Varsayilan davranis daima manuel tetikleme olacak; ajan arka planda veya proaktif sekilde Apify kosusu baslatmayacak.

## Instagram tarama cizgisi
- Kurucu yeni net kural verdi: Instagram scrape, isletmenin Instagram hesabini bulmak icin kullanilmayacak.
- Instagram kaynagi yalnizca bilinen profil linki varsa o profilden veri cekme amaciyla kullanilacak; aksi halde profil arama yapilmayacak.
- Bu cizgi yanlis eslesme riskini azaltmak icin business detail derin tarama akisine islendi.

## Teklif ve veri siniri kanoniklestirme
- `OFFERS.md` kanonik teklif omurgasi olarak yeniden yazildi; `Audit -> Paket 0 -> Paket 1-3 -> Paket 4 -> Bakim` ayrimi sertlestirildi, premium fikirler ve genel CRM kaymasi cekirdek omurgadan ayiklandi. Commit: `5aa9683` (`Canonicalize offer framework`).
- `REFERENCES/business-detail-v1.md` ve `REFERENCES/project-os-page.md` icinde `Business Detail` ile `Project OS` siniri netlestirildi; `canonical / external / derived` veri ayrimi bu yuzeyin ana karar modeli olarak sabitlendi. Commit: `1e5964d` (`Canonicalize Business Detail data boundaries`).
- Business Detail ekrani bu veri katmanlarini gorunur gosterecek sekilde yenilendi; `Hazirlik / Tarama` bolumunde hafif tarama ilk operator akisi olarak one alindi ve canli `3012` runtime smoke test ile dogrulandi. Commit: `6ebce80` (`Open light scan first in Business Detail`).
- Sonraki mantikli urun adimi, hafif tarama sonrasi `Y.Z raporu` ve karar uretim aksiyonunu ayni operator akisi icinde birbirine daha siki baglamak.

## Businesses / Project OS navigasyon sadeleştirmesi
- `businesses` listesindeki `Profili aç` ve `İş Takibinde aç` linkleri kaldırıldı; işletme adına tıklama doğrudan profil detayına gider hale getirildi.
- Aynı sadeleştirme `Project OS` kartlarında da uygulandı; gereksiz tekrar navigasyonu temizlendi.
- Değişiklik canlı admin runtime'a deploy edildi.

## Tarama detayı sayfası
- Business detail içindeki `Hazırlık / Tarama` alanı için ayrı `Tarama detayı` sayfası açıldı.
- Bu yüzeyde mevcut discovery snapshot, ajan tarama geçmişi, hafif/derin tarama geçmişi ve ham JSON verileri tek yerde görünür hale getirildi.

## Consultation prompt üretim hattı stabilizasyonu
- Prompt üretme ajanında `gpt-5-pro` için timeout çizgisi uzatıldı; kısa timeout yüzünden erken düşen üretim akışı düzeltildi.
- Aynı consultation kaydı için paralel prompt üretimini engelleyen server-side kilit eklendi; üst üste yazan ve kaydı kirleten tekrar çağrılar kesildi.
- Takılı prompt isteklerinde admin servis restart ile temiz kapanış alındı.

## Consultation prompt builder skill sadeleştirmesi
- `consultation-prompt-builder` skill'i sadeleştirildi; reference yapısı 4 dosyadan 2 dosyaya indirildi.
- `context-selection-matrix` içeriği `context-injection-policy` içine, `grounding-checklist` içeriği `prompting-principles` içine merge edildi.
- EsnafDigital repo linki `https://github.com/sahinseyfi/EsnafDijital-Openclaw` final prompt içinde zorunlu çizgi olarak korundu.
- `https://github.com/openclaw/openclaw` referansı ise yalnızca runtime/upstream davranışı gerçekten görevi etkiliyorsa eklenen koşullu çizgiye çekildi.
- Sadeleştirme commit'i: `ab37e94`.

## Consultation prompt sistemi için tasarım içgörüsü
- Mevcut prompt builder geniş kullanıcı sorularını daraltmaya çalışıyor ama güvenilir bir `decomposition` katmanı henüz yok.
- Single-task enforcement var, fakat çok parçalı soruları `primary task / secondary tasks / parked questions` biçiminde sistematik ayıran açık bir ara kontrat yok.
- Sonraki anlamlı iyileştirme, final prompttan önce scope reduction ve soru parçalama katmanı eklemek olacak.


## Consultation prompt decomposition karari
- Consultation Center prompt üretiminde varsayılan çizgi `tek prompt` olarak netleştirildi; geniş kullanıcı notu gelse bile sistem önce notu parçalayacak, sonra yalnızca `primaryTask` için tek `finalPromptText` üretecek.
- Çok parçalı notlarda otomatik çoklu prompt üretimi yok; bunun yerine `secondaryTasks`, `parkedQuestions` ve `promptStrategy=split_recommended` ile ayrıştırma yapılacak.
- `consultation-prompt-builder` skill kontratı buna göre güncellendi; yeni zorunlu alanlar `primaryTask`, `secondaryTasks`, `parkedQuestions`, `whyPrimaryNow`, `promptStrategy` olarak eklendi.
- `references/prompting-principles.md` ve `references/context-injection-policy.md` aynı çizgiye çekildi: önce decomposition, sonra `primaryTask` için bağlam seçimi.
- Consultation agent parser ve prompt summary tarafı da bu kontrata göre güncellendi. Commit: `1accf01` (`Add decomposition contract to consultation prompts`).

## Ana dosyalarda CRM-evrilebilir yön düzeltmesi
- Kurucu geri bildirimiyle ana dosyalardaki sınırlayıcı dil tekrar temizlendi; `Audit -> Teklif -> Teslimat -> Bakım` artık sistemin tek zorunlu hattı değil, ilk hizmet omurgası olarak tanımlandı.
- `PROJECT.md`, `ROADMAP.md`, `MEMORY.md`, `MEMORY_SUMMARY.md` ve `HEARTBEAT.md` buna göre düzeltildi.
- Yeni kalıcı çizgi: panel dar operasyon masası değil, EsnafDigital için CRM-evrilebilir operasyon sistemi.

## Hafif tarama arama saglayicisi duzeltmesi
- Hafif taramadaki Google arama hatti `Serper` degil `SerpApi` uzerinden kanoniklestirildi; `serpapi.com` playground ile dogrulanan key nedeniyle servis yanlis tespit edilmis oldu.
- Canli env cizgisi workspace disinda tutuluyor: `/etc/esnafdijital/esnafdijital-admin.env` icinde `SERPAPI_API_KEY` kullaniliyor; gizli deger workspace'e yazilmiyor.
- Kod tarafinda hafif `google-search` / `serp-signals` entegrasyonu `SerpApi` helper'ina cevrildi, ham smoke test `200` ve organic sonuc ile dogrulandi.
- Production build alinip `esnafdijital-admin.service` restart edildi; canli admin runtime yeni hafif tarama arama hattiyla deploy edildi. Commit: `8a98aab` (`Switch light scan search to SerpApi`).
